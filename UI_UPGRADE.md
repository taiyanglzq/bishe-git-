# 智慧校园助手前端 UI 升级文档

## 升级概览

- **项目名称**：个性化智慧校园助手（基于 SpringBoot + Vue 的毕设项目）
- **前端技术栈**：Vue 3 + Vite + Element Plus + ECharts + Pinia + SCSS + Tailwind CSS
- **升级时间**：2026-06-03
- **升级范围**：11 个页面 + 布局 + 全局样式 + 交互增强

---

## 一、设计系统

### 1.1 色彩变量（CSS Custom Properties）

建立了一套完整的 CSS 变量系统，统一管理颜色、阴影、圆角、过渡动画：

| 类别 | 变量 | 默认值 |
|---|---|---|
| 主色 | `--primary` | `#059669`（Emerald Green 学术绿） |
| 主色悬停 | `--primary-hover` | `#047857` |
| 主色激活 | `--primary-active` | `#065f46` |
| 背景色 | `--bg` | `#f1f5f9`（Slate 浅灰） |
| 表面色 | `--surface` | `#ffffff` |
| 文字色 | `--text` | `#0f172a` |
| 次要文字 | `--text-secondary` | `#475569` |
| 弱化文字 | `--text-muted` | `#94a3b8` |
| 边框色 | `--border` | `#e2e8f0` |

### 1.2 侧边栏配色

| 变量 | 值 | 说明 |
|---|---|---|
| `--sidebar-bg` | `#0f172a` | 暗色 Slate 背景，与主内容区形成对比 |
| `--sidebar-text` | `#94a3b8` | 菜单项文字 |
| `--sidebar-active-bg` | `#059669` | 当前激活项—主色高亮 |

### 1.3 语义色

| 变量 | 值 | 用途 |
|---|---|---|
| `--success` | `#10b981` | 成功操作 |
| `--warning` | `#f59e0b` | 警告提示 |
| `--danger` | `#ef4444` | 危险操作 / 删除 |
| `--info` | `#3b82f6` | 信息提示 |

### 1.4 阴影层级

```
--shadow-xs → --shadow-sm → --shadow-md → --shadow-lg → --shadow-xl
```
卡片默认使用 `--shadow-sm`，悬浮时升级为 `--shadow-md`。

### 1.5 圆角系统

```
--radius-sm: 6px    → 按钮、输入框、标签
--radius-md: 10px   → 小卡片
--radius-lg: 14px   → 标准卡片
--radius-xl: 20px   → 大卡片、弹窗
```

---

## 二、布局升级

### 2.1 AdminLayout（文件：`src/layout/AdminLayout.vue`）

**改动前**：左侧浅色侧边栏，无图标，无折叠功能。

**改动后**：
- 侧边栏改为**暗色背景（#0f172a）**，菜单项带 Element Plus 图标
- 支持**折叠/展开**（按钮在顶栏左侧），折叠后仅显示图标
- 顶栏右侧：**通知铃铛**（带未读红点）+ **用户下拉菜单**（头像圆圈首字母 + 姓名 + 角色 + 下拉选项）
- **面包屑导航**：自动显示当前路由标题
- 页面切换有 **fade-slide 过渡动画**
- 菜单按角色权限分为"主导航"和"系统管理"两组
- 增加"讨论交流"菜单入口（路由 `/discussion`）

```
路由懒加载：除登录页外，所有页面组件均使用 () => import(...) 动态导入
```

---

## 三、页面逐页升级明细

### 3.1 登录页（`src/views/login/LoginView.vue`）

| 改动项 | 说明 |
|---|---|
| 布局 | **左右分栏**：左侧绿色渐变品牌区（Slogan + 功能介绍图标），右侧白色表单面板 |
| 表单校验 | 账号必填（≥3位）、密码必填（≥6位），实时校验 + 错误提示 |
| 记住账号 | 勾选后 localStorage 保存用户名，下次打开自动填入；不勾选则清除 |
| 按钮状态 | 登录中显示 loading 转圈 + "登录中…"文字 |
| 默认账号 | 首次或无记住时，预填 `23050539414` / `123456` |

### 3.2 首页工作台（`src/views/dashboard/DashboardView.vue`）

| 改动项 | 说明 |
|---|---|
| Hero 区 | 绿色渐变横幅 + 分时段问候语（早上好/下午好/晚上好）+ 快捷操作按钮 |
| 统计卡片 | 6 个指标卡片（用户总数 / 今日预约 / 未读通知 / 待审预约 / 活动总数 / 签到人次），每张带彩色图标 |
| 待办 + 日程 | 双栏面板，每条待办带左侧红点指示器、类型标签、时间文字 |
| ECharts 图表 | 4 个图表区——预约状态饼图（环形）、活动报名柱状图、场地预约柱状图、签到率仪表盘（Gauge） |
| 图表配色 | 统一 Emerald Green 色阶，圆角柱状图，白色分隔线 |

### 3.3 校园公告（`src/views/notice/NoticeView.vue`）

| 改动项 | 说明 |
|---|---|
| 卡片布局 | 从裸表格改为 **info-grid 卡片网格**，每张卡片含标题、分类标签、摘要、浏览量、时间 |
| 搜索筛选 | 关键词搜索 + 分类下拉（系统通知/学术活动/校园新闻/考试信息），前端实时过滤 |
| 详情弹窗 | 点击卡片弹出 Dialog，展示完整内容 + **评论交流区**（发表评论、评论列表含头像/角色/院系/时间） |
| 分页 | 客户端分页（9 条/页），搜索/筛选变化时自动回到第 1 页 |
| 空状态 | 无数据时显示 Document 图标 + "暂无公告信息" |

### 3.4 场地资源（`src/views/venue/VenueView.vue`）

| 改动项 | 说明 |
|---|---|
| 卡片布局 | **venue-card 卡片网格**，每张卡片含场地名称、位置图标、容量、可用/停用状态标签 |
| 搜索 | 场地名称搜索，前端实时过滤 |
| 悬停效果 | 卡片悬停上浮（translateY -2px）+ 阴影加深 |

### 3.5 场地预约（`src/views/booking/BookingView.vue`）

| 改动项 | 说明 |
|---|---|
| 场地选择 | 左侧场地列表，每项含**缩略图**（有图显示图，无图显示"场地"占位符）、名称、位置、容量 |
| 时间段选择 | 右侧 Grid 布局，每个时间段独立按钮，显示时间 + 剩余/总量，已满自动 disabled |
| 预约提交 | 原因输入框 + 提交按钮，必选项未填时按钮 disabled |
| 状态标签 | 表格中状态列使用彩色标签（绿=已通过/红=已驳回/灰=已取消/橙=待审核） |
| 二次确认 | **驳回预约**和**取消预约**均弹出确认对话框 |
| 图片适配 | 场地图片显示支持绝对 URL 和相对路径 |

### 3.6 活动与签到（`src/views/activity/ActivityView.vue`）

| 改动项 | 说明 |
|---|---|
| 活动卡片 | 卡片列表：标题 + 地点图标 + 状态标签 + 活动说明 + 4 格信息区（活动时间/签到时间/已报名/状态） |
| 操作按钮 | 未报名→"报名"按钮 / 已报名→"取消报名"+"签到" / 已签到→绿色标签 |
| 我的报名/签到 | 两个子 Tab 用表格展示报名和签到记录 |

### 3.7 讨论交流（`src/views/discussion/DiscussionView.vue`）

| 改动项 | 说明 |
|---|---|
| 发布帖子 | 标题输入 + 文本域（500 字限制）+ 图片上传（模拟） |
| 帖子列表 | 卡片列表：置顶/精华标签、角色标签、标题、作者信息，缩略图，点赞数/评论数 |
| 详情弹窗 | 点击展开 Dialog：完整内容 + 大图 + 评论区（发表评论、点赞评论、删除评论） |
| 管理功能 | 帖子置顶/精华切换，封禁作者发帖评论 |
| 二次确认 | **删除帖子、删除评论、封禁用户**均弹出确认对话框 |
| 分页 | 后端分页（10 条/页），搜索时回到第 1 页 |

### 3.8 个性化推荐（`src/views/recommendation/RecommendationView.vue`）

| 改动项 | 说明 |
|---|---|
| 推荐依据 | Alert 区域以多个绿色 Tag 展示推荐理由 |
| 三栏推荐 | 推荐公告（info-card 卡片）/ 推荐活动（info-card 卡片）/ 推荐场地（info-card 卡片） |
| 空状态 | 每个栏目无数据时显示 mini Empty 占位 |

### 3.9 通知中心（`src/views/notification/NotificationView.vue`）

| 改动项 | 说明 |
|---|---|
| 通知列表 | 每项左对齐：未读→绿色圆点，已读→灰色 Check 图标；未读项有左侧绿色边框 + 浅绿背景 |
| 操作区 | 阅读状态筛选 + 刷新 + "全部已读"按钮 |
| 标记已读 | 单条已读按钮，点击后刷新列表 |

### 3.10 个人中心（`src/views/profile/ProfileView.vue`）

| 改动项 | 说明 |
|---|---|
| 双栏布局 | 左侧：头像圆圈 + 姓名 + 角色徽章 + 账号/学号/院系/初始密码信息列表 |
| 修改密码 | 右侧：旧密码 + 新密码 + 确认密码，三字段均带表单校验 |
| 校验规则 | 旧密码必填、新密码≥6位、确认密码需与新密码一致（自定义 validator） |
| 按钮状态 | loading 转圈 + 成功后自动清空表单并清除校验状态 |

### 3.11 日志审计（`src/views/log/LogView.vue`）

| 改动项 | 说明 |
|---|---|
| 分页 | 后端分页（默认 20 条/页，支持 10/20/50 切换），带总数显示 |
| 操作标签 | 操作列根据操作类型自动着色（新增/删除/更新/审核等） |
| 业务类型 | 业务类型列使用 plain 风格标签 |
| 搜索栏 | 保留关键词 + 业务类型筛选框和刷新按钮 |

### 3.12 系统管理（`src/views/system/SystemView.vue`）

| 改动项 | 说明 |
|---|---|
| Tab 导航 | 5 个 Tab（用户/公告/场地/时间段/活动），每个带图标 |
| 表单内联 | 每个 Tab 顶部独立表单行，填入→点击新增/更新 |
| 表单校验 | 提交前检查必填字段（标题/内容/场地/日期等），缺失则 warning 提示 |
| 二次确认 | **所有删除操作**（用户/公告/场地/时间段/活动）均弹出确认对话框 |
| 分页 | 每个 Tab 表格独立后端分页（10 条/页） |
| 场地下拉 | 场地管理表格分页与活动/时间段表单的全量场地下拉分离（`venueOptions` vs `venues`） |
| 图片上传 | 场地管理支持缩略图上传（`image-uploader` 内联组件），活动管理支持海报上传 |
| 表格缩略图 | 场地表格显示场地缩略图，活动表格显示海报缩略图 |

---

## 四、交互增强汇总

### 4.1 二次确认（ElMessageBox.confirm）

| 页面 | 操作 | 确认文案 |
|---|---|---|
| 场地预约 | 驳回预约 | "确定驳回该预约申请吗？" |
| 场地预约 | 取消预约 | "确定取消该预约吗？名额将释放。" |
| 系统管理 | 删除用户/公告/场地/时间段/活动 | "确定删除该 XXX 吗？此操作不可恢复。" |
| 讨论交流 | 删除帖子 | "确定删除该帖子吗？此操作不可恢复。" |
| 讨论交流 | 删除评论 | "确定删除该评论吗？" |
| 讨论交流 | 封禁用户 | "确定封禁该用户的发帖和评论权限吗？" |

### 4.2 分页（ElPagination）

| 页面 | 类型 | 每页条数 | 支持每页数量切换 |
|---|---|---|---|
| 校园公告 | 客户端分页 | 9 条 | 否（网格布局，不适合） |
| 日志审计 | 后端分页 | 10/20/50 | 是 |
| 系统管理（5 表） | 后端分页 | 10 条 | 否 |
| 讨论交流 | 后端分页 | 10 条 | 否 |

### 4.3 表单校验

| 页面 | 校验方式 | 校验规则 |
|---|---|---|
| 登录页 | el-form rules | 账号必填 ≥3 位，密码必填 ≥6 位 |
| 个人中心 | el-form rules | 旧密码必填，新密码 ≥6 位，确认密码与之一致 |
| 系统管理 | submit 函数内 ElMessage.warning | 标题/登录号/姓名/场地名称/内容等必填检查 |
| 讨论区发帖 | 计算属性 disabled | 标题 + 内容均非空才可提交 |

### 4.4 记住账号

登录页支持勾选"记住账号"，保存用户名到 localStorage。下次打开时自动填入，密码不保存。

---

## 五、性能优化

| 优化项 | 说明 |
|---|---|
| 路由懒加载 | 所有页面组件使用 `() => import(...)` 动态导入，构建产物拆分为独立 chunk |
| Tailwind CSS | 按需生成样式，`preflight: false` 避免与 Element Plus 样式冲突 |
| 包体积 | 构建后 CSS 53KB (gzip)、JS 分片加载，首屏最小化 |

---

## 六、文件改动清单

| 文件 | 改动类型 |
|---|---|
| `package.json` | 新增依赖：tailwindcss, postcss, autoprefixer |
| `tailwind.config.js` | 新增：Tailwind 配置 |
| `postcss.config.js` | 新增：PostCSS 配置 |
| `src/styles/main.scss` | 全面重写 + 追加分页/登录样式 |
| `src/layout/AdminLayout.vue` | 暗色侧边栏 + 图标 + 折叠 + 用户下拉 + 面包屑 |
| `src/router/index.js` | 路由懒加载 + 讨论交流路由 |
| `src/views/login/LoginView.vue` | 左右分栏 + 表单校验 + 记住账号 |
| `src/views/dashboard/DashboardView.vue` | Hero + 统计卡片 + ECharts 升级 |
| `src/views/notice/NoticeView.vue` | 卡片网格 + 搜索 + 详情弹窗 + 评论 + 分页 |
| `src/views/venue/VenueView.vue` | 卡片网格 + 搜索 |
| `src/views/booking/BookingView.vue` | 场地图片 + 二次确认（驳回/取消） |
| `src/views/activity/ActivityView.vue` | 活动卡片重设计 |
| `src/views/discussion/DiscussionView.vue` | 新增：讨论交流完整页面（发帖/评论/点赞/管理） |
| `src/views/recommendation/RecommendationView.vue` | 三栏卡片推荐 |
| `src/views/notification/NotificationView.vue` | 未读圆点 + 绿色边框 |
| `src/views/profile/ProfileView.vue` | 双栏 + 表单校验 + 确认密码 |
| `src/views/log/LogView.vue` | 后端分页 + 每页数量切换 |
| `src/views/system/SystemView.vue` | 图片上传 + 表单校验 + 二次确认 + 5 表分页 |
| `src/api/discussion.js` | 新增：讨论区 API |
| `src/api/notice.js` | 新增：评论 API |
| `src/api/venue.js` | 新增：图片上传 API |
| `vite.config.js` | 新增 `/uploads` 代理 |

---

## 七、启动说明

```bash
# 安装依赖
cd frontend && npm install

# 开发模式（端口 5173）
npm run dev

# 生产构建
npm run build
```

> 后端需运行在 8085 端口。前端 Vite 代理将 `/api` 转发至后端，`/uploads` 直传静态资源。
