# 智慧校园助手性能优化总结

## 1. 文档目的

本文档用于总结“基于 Spring Boot + Vue 的个性化智慧校园助手”项目在后端性能方面已经完成的优化工作，重点说明：

- 原始性能问题
- 优化思路
- 已落地的技术方案
- 各模块优化结果
- 答辩时可直接说明的技术亮点

本文档适合用于：

- 毕设答辩讲解
- 开发过程复盘
- 后续继续优化时的基线说明

---

## 2. 原始性能问题

在未进行性能优化之前，项目主要存在以下问题：

### 2.1 数据库直连问题

大部分高频读接口每次请求都会直接访问 MySQL，例如：

- 首页数据看板
- 公告列表
- 公告详情
- 场地列表
- 活动列表
- 推荐结果
- 通知中心
- 讨论区帖子列表

这会导致：

- 重复查询过多
- 高并发场景下数据库压力增大
- 接口响应时间不稳定

### 2.2 N+1 查询问题

部分聚合接口存在典型 N+1 问题，例如：

- 公告详情中评论列表逐条查询评论用户
- 活动列表逐条查询当前用户是否已报名、是否已签到
- 我的报名记录逐条查询活动信息
- 我的签到记录逐条查询活动信息
- 讨论区帖子列表逐条查询作者、评论、评论用户、点赞状态

这会导致：

- 一次分页请求触发大量 SQL
- 数据量增大时接口性能明显下降

### 2.3 缓存失效逻辑分散

在逐步给项目加缓存后，如果每个业务类各自维护删缓存逻辑，会出现：

- 容易漏删缓存
- 容易删多或删少
- 后续维护成本快速上升
- 很难清晰说明缓存一致性策略

### 2.4 缺少可观测性

如果只写缓存代码而没有日志，就很难证明：

- 接口是否命中了缓存
- 缓存是否真正生效
- 缓存失效后是否成功回源并重建

---

## 3. 优化总体思路

本项目采用了“先建立缓存基础设施，再逐步改造热点模块，最后统一缓存失效逻辑”的优化路线。

总体思路如下：

1. 先做缓存设计清单，明确哪些数据适合缓存。  
2. 新增统一缓存工具类，避免 Redis 操作散落在业务代码中。  
3. 优先改造高频读取接口，如首页看板、公告、场地、活动、推荐。  
4. 修复各模块中的 N+1 查询问题。  
5. 将缓存失效逻辑统一收口到独立服务中。  
6. 增加缓存命中日志，提升可观测性。  

---

## 4. 已落地的优化内容

### 4.1 缓存设计清单

已新增：

- [缓存设计清单.md](D:/code/ideajavacode/hmdp-plus/bishe/缓存设计清单.md)

清单中明确了：

- 哪些模块缓存
- Key 如何命名
- TTL 如何设置
- 写操作后如何删除缓存
- 哪些数据允许短暂不一致

这一步的价值在于：

- 避免后续缓存越做越乱
- 让缓存设计可答辩、可复盘

---

### 4.2 缓存基础设施

已新增：

- [CacheKeyConstants.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/common/cache/CacheKeyConstants.java)
- [CacheClient.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/common/cache/CacheClient.java)

#### 作用

`CacheKeyConstants` 用于统一管理缓存前缀，避免硬编码。  
`CacheClient` 用于统一封装：

- 对象缓存读写
- 字符串缓存读写
- 空值缓存
- 单个删除
- 批量删除
- Key 扫描

#### 技术意义

- 统一 Redis 使用方式
- 减少重复样板代码
- 为后续缓存改造提供统一入口

---

### 4.3 首页数据看板缓存

优化文件：

- [DashboardServiceImpl.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/service/impl/DashboardServiceImpl.java)

#### 已做优化

- `stats()` 按角色维度缓存，TTL 为 5 分钟
- `workbench()` 按用户维度缓存，TTL 为 3 分钟
- `summary()` 直接复用 `stats()` 中的 summary，避免重复统计

#### 解决的问题

- 首页每次都重新统计全站数据
- 同一角色用户频繁访问导致重复聚合查询
- 工作台待办和统计卡片数据库压力大

#### 当前效果

- 首页热点统计改为优先命中 Redis
- 同一时间窗口内相同角色访问不再重复查库

---

### 4.4 公告模块缓存与详情优化

优化文件：

- [NoticeController.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/controller/NoticeController.java)

#### 已做优化

- 公告分页接口 `GET /notice/page` 接入 Redis 缓存
- 公告详情接口 `GET /notice/{id}` 接入 Redis 缓存
- 评论新增后删除对应公告详情缓存
- 公告新增、修改、删除后删除分页缓存和详情缓存

#### N+1 优化

原逻辑：

- 每条评论单独查询一次评论用户

现逻辑：

- 先查评论列表
- 批量查询评论用户
- 内存组装 `NoticeCommentVO`

#### 当前效果

- 公告列表重复读取压力下降
- 公告详情查询次数下降
- 评论区聚合效率提升

---

### 4.5 场地列表缓存

优化文件：

- [VenueController.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/controller/VenueController.java)

#### 已做优化

- 场地分页接口 `GET /venue/page` 接入缓存
- TTL 为 15 分钟
- 场地新增、修改、删除后自动清理场地缓存
- 同时联动清理推荐缓存和首页看板缓存

#### 当前效果

- 场地展示页从“每次直连数据库”改为“优先命中缓存”

---

### 4.6 活动列表缓存与状态批量查询优化

优化文件：

- [ActivityBizServiceImpl.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/service/impl/ActivityBizServiceImpl.java)

#### 已做缓存

- 活动列表按 `current + size + role + college + userId` 维度缓存
- TTL 为 10 分钟
- 报名、取消报名、签到后删除活动列表缓存

#### 已做 N+1 优化

原逻辑：

- 每个活动逐条查询是否已报名
- 每个活动逐条查询是否已签到

现逻辑：

- 先收集当前页活动 ID
- 批量查询当前用户已报名活动
- 批量查询当前用户已签到活动
- 内存组装 `ActivityVO`

#### 当前效果

- 活动列表查询 SQL 次数显著下降
- 缓存失效后活动列表回源成本也更低

---

### 4.7 推荐模块缓存

优化文件：

- [RecommendationController.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/controller/RecommendationController.java)

#### 已做优化

- `GET /recommendation/personal` 按用户 ID 缓存
- TTL 为 10 分钟
- 场地、活动、公告变更时联动删除推荐缓存

#### 当前效果

- 推荐页从“每次动态拼装”改为“优先使用缓存结果”
- 推荐结果的构建开销降低

---

### 4.8 通知中心短缓存

优化文件：

- [NotificationController.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/controller/NotificationController.java)
- [NotificationServiceImpl.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/service/impl/NotificationServiceImpl.java)

#### 已做优化

- `GET /notification/page` 按用户分页维度缓存，TTL 为 2 分钟
- `GET /notification/unread-count` 按用户维度缓存，TTL 为 2 分钟
- 新通知、单条已读、全部已读后自动清理缓存

#### 当前效果

- 通知中心高频刷新时数据库压力下降
- 页面角标未读数查询更轻量

---

### 4.9 活动记录 N+1 优化

优化文件：

- [ActivityBizServiceImpl.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/service/impl/ActivityBizServiceImpl.java)

#### 已做优化

- `myEnrollments()` 批量查询活动信息
- `myCheckins()` 批量查询活动信息

#### 原问题

- 每条报名记录逐条 `selectById(activity)`
- 每条签到记录逐条 `selectById(activity)`

#### 当前效果

- 我的活动记录页数据库访问次数明显减少

---

### 4.10 讨论区分页缓存与多层 N+1 优化

优化文件：

- [DiscussionController.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/controller/DiscussionController.java)

#### 已做缓存

- `GET /discussion/page` 接入短缓存
- 按 `current + size + keyword + userId` 维度缓存
- TTL 为 5 分钟

#### 原始问题

讨论区原先存在多层 N+1：

- 每条帖子查作者
- 每条帖子查评论
- 每条评论查评论用户
- 每条评论查点赞数
- 每条帖子和评论再查当前用户点赞状态

#### 当前优化方式

- 批量查帖子作者
- 批量查评论
- 批量查评论用户
- 批量查帖子点赞关系
- 批量查评论点赞关系
- 批量统计评论点赞数
- 最后统一组装 `DiscussionPostVO`

#### 当前效果

- 讨论区分页接口的数据库访问次数显著下降
- 即使缓存失效，数据库回源成本也比以前低很多

---

### 4.11 缓存失效统一收口

新增文件：

- [CacheEvictService.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/service/CacheEvictService.java)
- [CacheEvictServiceImpl.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/service/impl/CacheEvictServiceImpl.java)

#### 目的

把分散在多个 Controller / ServiceImpl 中的删缓存逻辑统一收口。

#### 当前统一管理的缓存

- 首页统计缓存
- 首页工作台缓存
- 推荐缓存
- 场地缓存
- 公告分页缓存
- 公告详情缓存
- 活动缓存
- 通知分页缓存
- 通知未读数缓存
- 讨论区分页缓存

#### 当前效果

- 新增写操作时更不容易漏删缓存
- 缓存一致性策略更清晰
- 代码维护成本下降

---

### 4.12 缓存命中日志

优化文件：

- [CacheClient.java](D:/code/ideajavacode/hmdp-plus/bishe/backend/src/main/java/com/campus/assistant/common/cache/CacheClient.java)

#### 已增加日志类型

- `CACHE_HIT`
- `CACHE_MISS`
- `CACHE_HIT_NULL`
- `CACHE_SET`
- `CACHE_SET_NULL`
- `CACHE_DELETE`
- `CACHE_DELETE_BATCH`
- `CACHE_DESERIALIZE_FAIL`

#### 当前作用

- 可直接在控制台观察缓存是否命中
- 可以作为答辩时的技术证明
- 后续若补接口耗时统计，也有统一基础

---

## 5. 当前优化结果总结

截至目前，项目已经从“几乎所有热点接口都直连数据库”的状态，优化为：

### 已接入缓存的模块

- 首页看板
- 首页工作台
- 公告分页
- 公告详情
- 场地分页
- 活动分页
- 推荐结果
- 通知分页
- 通知未读数
- 讨论区分页

### 已修复或明显缓解 N+1 的模块

- 公告评论区
- 活动列表
- 我的报名记录
- 我的签到记录
- 讨论区帖子分页

### 已完成的缓存一致性能力

- 统一缓存工具
- 统一缓存 Key 管理
- 统一缓存失效收口
- 统一缓存命中日志

---

## 6. 答辩时可以怎么讲

答辩时可以按下面这条主线说明：

### 6.1 原始问题

系统初期存在三个明显问题：

1. 高频读接口全部直连 MySQL。  
2. 部分聚合接口存在 N+1 查询。  
3. 缓存失效逻辑若分散维护，后期容易混乱。  

### 6.2 优化路线

针对以上问题，我采取了分阶段优化方式：

1. 先制定缓存设计清单。  
2. 再实现统一缓存基础设施。  
3. 然后优先优化首页看板、公告、场地、活动、推荐、通知、讨论区等高频模块。  
4. 再修复 N+1 查询。  
5. 最后将缓存失效统一收口，并增加缓存命中日志。  

### 6.3 具体技术点

可以重点提下面这些技术关键词：

- Redis 缓存
- 缓存 Key 统一设计
- 短 TTL 缓存
- 更新数据库后删除缓存
- 批量查询替代逐条查询
- N+1 查询优化
- 缓存命中日志
- 统一缓存失效服务

### 6.4 优化收益

可以这样讲：

- 高频接口不再每次都直连数据库。  
- 首页看板、公告、活动、推荐、通知等接口的重复查询压力明显降低。  
- 讨论区、公告详情、活动记录等模块的 N+1 查询问题已得到修复或缓解。  
- 缓存一致性和后续维护性明显提升。  

---

## 7. 后续可继续优化的方向

虽然当前优化已经较完整，但仍有后续可扩展空间：

1. 增加接口耗时日志。  
2. 增加缓存命中率统计。  
3. 为预约分页和审核分页增加短缓存。  
4. 为场地时间段快照增加短 TTL 缓存。  
5. 将部分批量组装逻辑进一步下沉为自定义 SQL 聚合查询。  

---

## 8. 当前结论

本项目目前已经形成了一条较完整、可落地、可答辩的性能优化链路：

- 从问题识别出发
- 到缓存设计
- 到缓存落地
- 到 N+1 修复
- 到缓存一致性收口
- 再到缓存命中可观测性

这使得项目不再只是“功能能跑”，而是具备了较明显的工程化优化思路，能够更有说服力地回答“为什么要这样设计”和“优化是否真正生效”。
