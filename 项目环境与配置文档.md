# 个性化智慧校园助手系统环境与配置文档

## 一、文档说明

本文档用于说明“基于 Spring Boot + Vue 的个性化智慧校园助手的设计与实现”项目在本地开发、运行、联调和部署前需要准备的软件环境、账号信息、配置文件、数据库、Redis、Kafka、Redisson 以及 AI 配置等内容。

项目采用前后端分离架构，分为两个工程：

```text
backend   后端项目
frontend  前端项目
```

系统面向学生、教师、管理员三类角色，核心功能包括登录认证、权限控制、校园公告、场地预约、活动报名、签到管理、个性化推荐、数据看板、操作日志、消息通知以及 AI 客服与讨论区内容审核。

## 二、本地开发环境要求

### 2.1 必需安装的软件

| 软件 | 推荐版本 | 用途 |
| --- | --- | --- |
| JDK | 17+ | 后端运行环境 |
| Maven | 3.8+ | 后端依赖管理与打包 |
| Node.js | 18+ | 前端运行环境 |
| npm / pnpm | npm 9+ / pnpm 8+ | 前端依赖管理 |
| MySQL | 8.x | 业务数据存储 |
| Git | 最新稳定版 | 版本管理 |
| IntelliJ IDEA | 2023+ | 后端开发 |
| VS Code | 最新稳定版 | 前端开发 |
| Apifox / Postman | 最新稳定版 | 接口调试 |
| Navicat / DataGrip | 最新稳定版 | 数据库管理 |

### 2.2 Docker 部署的软件

除 MySQL 外，本项目的中间件默认通过 Docker 部署。

| 软件 | 推荐版本 | 用途 |
| --- | --- | --- |
| Docker Desktop | 最新稳定版 | 运行 Redis、Kafka 等容器 |
| Docker Compose | 最新稳定版 | 编排 Redis、Kafka 容器 |
| Redis | 6.x / 7.x | 缓存、幂等控制、分布式锁依赖 |
| Kafka | 3.x | 异步消息、通知、日志、补偿 |
| RedisInsight | 最新稳定版 | Redis 可视化管理，可选 |
| Kafka Tool / Offset Explorer | 最新稳定版 | Kafka 消息查看，可选 |
| Nginx | 1.20+ | 生产部署反向代理，可选 |

## 三、账号与连接信息

| 类型 | 是否必须 | 用途 |
| --- | --- | --- |
| MySQL 用户名和密码 | 是 | 后端连接业务数据库 |
| Redis 连接信息 | 是 | 后端连接 Docker 中的 Redis |
| Kafka 连接信息 | 是 | 后端连接 Docker 中的 Kafka |
| AI API Key | 否 | 启用 AI 客服与 AI 审核时使用 |
| 后端 API 地址 | 是 | 前端开发联调 |

说明：

- 本项目默认本机安装 MySQL，Redis 和 Kafka 通过 Docker 启动。
- Redis、Kafka 的端口、密码和访问地址必须与 Docker 配置保持一致。
- 生产环境不应将数据库密码、Redis 密码、Kafka 密钥写死在代码或公开仓库中。
- 如需启用 AI 功能，需要额外配置 DeepSeek API Key。

## 四、MySQL 配置

### 4.1 创建数据库

```sql
CREATE DATABASE IF NOT EXISTS campus_assistant
DEFAULT CHARACTER SET utf8mb4
DEFAULT COLLATE utf8mb4_general_ci;
```

### 4.2 数据库命名规范

```text
表名统一使用 ca_ 前缀
主键统一使用 BIGINT
逻辑删除字段统一使用 deleted
状态字段统一使用 status
创建时间字段统一使用 create_time
更新时间字段统一使用 update_time
不强制使用数据库物理外键，通过字段进行逻辑关联
```

### 4.3 初始化数据

建议初始化以下基础数据：

- 学生、教师、管理员三类角色
- 学生基础账号，账号默认为学号，例如 `23050539414`，初始密码默认为身份证后 6 位
- 学生首次登录后允许修改密码，后端存储密码时必须使用加密后的密文，不允许明文保存
- 教师账号规则暂按“管理员后台创建或初始化脚本预置”处理，后续可根据学校实际管理方式调整
- 管理员账号规则暂按“初始化脚本预置超级管理员，再由管理员创建其他管理员”处理
- 校园公告分类
- 场地基础数据
- 活动基础数据
- 测试预约记录
- 测试签到记录

## 五、Redis 与 Redisson 配置

### 5.1 Redis 用途

```text
登录 Token 缓存
验证码缓存
用户信息缓存
公告详情缓存
活动详情缓存
首页数据看板缓存
接口限流
预约幂等控制
签到防重复提交
Kafka 消息消费去重
```

### 5.2 Redis Key 规范

```text
ca:login:token:{userId}
ca:captcha:{uuid}
ca:user:info:{userId}
ca:notice:{noticeId}
ca:activity:{activityId}
ca:booking:{bookingId}
ca:checkin:{checkinId}
ca:dashboard:summary
ca:rate:limit:{scene}:{userId}
ca:mq:msg:{messageId}
ca:lock:cache:rebuild:{bizType}:{bizId}
```

### 5.3 Redisson 用途

Redisson 作为 Redis 的增强组件，主要用于：

```text
分布式锁
缓存重建互斥
布隆过滤器
重复执行限制
延迟队列
```

其中，重复执行限制用于防止同一用户短时间内重复提交相同业务请求；延迟队列用于处理预约提醒、活动提醒、签到提醒、预约超时取消、缓存延迟删除和失败消息延迟补偿。

### 5.4 重复执行限制

推荐 Key 规范：

```text
ca:repeat:booking:{userId}:{venueId}:{bookingDate}:{timeRange}
ca:repeat:checkin:{userId}:{activityId}
ca:repeat:activity:enroll:{userId}:{activityId}
ca:repeat:audit:{operatorId}:{bookingId}:{auditAction}
ca:repeat:message:{messageId}
```

业务规则：

- 同一学生不能重复预约同一场地的同一时间段，避免重复提交、重复占用资源和名额统计异常。
- 同一学生不能重复签到同一活动，避免签到记录重复和参与统计异常。
- 同一学生不能重复报名同一活动，避免活动名额重复扣减。
- 同一教师或管理员不能重复审核同一预约单的同一审核动作。
- 同一 Kafka 消息不能重复触发同一业务处理。

### 5.5 延迟队列

推荐队列命名：

```text
ca:delay:booking-remind
ca:delay:activity-remind
ca:delay:checkin-remind
ca:delay:booking-timeout
ca:delay:cache-delete
ca:delay:message-compensate
```

典型流程：

```text
业务创建成功
  -> 计算延迟时间
  -> 写入 Redisson 延迟队列
  -> 到期后消费者取出任务
  -> 执行提醒、取消、缓存删除或补偿逻辑
```

## 六、Kafka 配置

### 6.1 Kafka 用途

```text
预约成功通知
预约审核结果通知
活动开始提醒
公告发布通知
操作日志异步落库
缓存失效广播
消息发送失败补偿
DLQ 死信队列重放
```

### 6.2 Topic 设计

```text
ca-notice-topic
ca-booking-topic
ca-checkin-topic
ca-log-topic
ca-cache-invalidate-topic
ca-notice-topic.DLQ
ca-booking-topic.DLQ
ca-checkin-topic.DLQ
ca-log-topic.DLQ
ca-cache-invalidate-topic.DLQ
```

### 6.3 消息体规范

Kafka 消息统一使用 JSON 格式，建议包含：

```text
messageId
bizType
bizId
operatorId
createTime
payload
```

### 6.4 消息幂等与 DLQ 补偿

消费端使用 Redis `SETNX` 对 `messageId` 去重：

```text
SETNX 成功 -> 执行业务逻辑
SETNX 失败 -> 说明消息已处理，直接跳过
```

失败处理原则：

- 生产者发送失败时记录失败日志，并尝试投递到对应 DLQ Topic。
- 消费者处理异常且超过重试次数后，将消息转发到 DLQ。
- DLQ 消费者负责补偿处理、人工排查和必要时重放。
- 失败消息必须保留 `messageId`、`bizType`、`bizId` 和失败原因。

## 七、后端配置文件

### 7.1 配置文件位置

```text
backend/src/main/resources/application.yml
backend/src/main/resources/application-dev.yml
```

### 7.2 application.yml

```yaml
spring:
  profiles:
    active: dev

server:
  port: 8085
```

### 7.3 application-dev.yml 示例

本项目当前不将分库分表作为一期必做功能，因此开发环境默认使用普通 MySQL 数据源。ShardingSphere 可作为后续扩展方案预留。

```yaml
server:
  port: 8085

spring:
  application:
    name: campus-assistant
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_assistant?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: your_mysql_password
  data:
    redis:
      database: 1
      host: localhost
      port: 6379
      password: 2004lzqlzq
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
    servlet:
      load-on-startup: 1
  jackson:
    default-property-inclusion: non_null
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
    generator:
      WRITE_NUMBERS_AS_STRINGS: true
  kafka:
    bootstrap-servers: 192.168.211.20:9092
    producer:
      retries: 1
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: campus_assistant_group
      enable-auto-commit: true
      auto-commit-interval: 200
      auto-offset-reset: latest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    prefix:
      distinction:
        name: ca

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.campus.assistant.entity
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    local-cache-scope: statement

logging:
  level:
    com.campus.assistant: info
    AUDIT: INFO
  pattern:
    dateformat: mm:ss.SSS

bloom-filter:
  filters:
    notice:
      name: notice-id-bloom
      expected-insertions: 50000
      false-probability: 0.01
    activity:
      name: activity-id-bloom
      expected-insertions: 50000
      false-probability: 0.01

management:
  endpoints:
    web:
      exposure:
        include: "prometheus,health,info"
  prometheus:
    metrics:
      export:
        enabled: true

app:
  ai:
    enabled: false
    provider: deepseek
    api-key:
    base-url: https://api.deepseek.com
    chat-model: deepseek-chat
    moderation-enabled: true
    assistant-enabled: true
```

### 7.4 AI 配置说明

| 配置项 | 说明 |
| --- | --- |
| `app.ai.enabled` | 是否启用 AI 模块总开关 |
| `app.ai.api-key` | DeepSeek API Key |
| `app.ai.base-url` | AI 服务地址，默认 `https://api.deepseek.com` |
| `app.ai.chat-model` | 对话模型名称，默认 `deepseek-chat` |
| `app.ai.moderation-enabled` | 是否启用内容审核 |
| `app.ai.assistant-enabled` | 是否启用 AI 客服助手 |

说明：

- 当 `app.ai.enabled=false` 或未配置 `api-key` 时，AI 客服助手将返回兜底回答。
- 内容审核模块在未配置外部模型时，仍会执行本地敏感词规则兜底。
- 当前后端 AI 对话接口为 `POST /ai/chat`。

## 八、前端配置文件

```text
frontend/.env.development
frontend/.env.production
```

### 8.1 .env.development

```env
VITE_APP_TITLE=个性化智慧校园助手
VITE_API_BASE_URL=http://localhost:8085
```

### 8.2 .env.production

```env
VITE_APP_TITLE=个性化智慧校园助手
VITE_API_BASE_URL=https://your-domain.com/api
```

前端只配置后端 API 地址，不保存 MySQL、Redis、Kafka 等敏感连接信息。

## 九、后端依赖清单

```text
spring-boot-starter-web
spring-boot-starter-validation
mybatis-plus-spring-boot3-starter
mysql-connector-j
spring-boot-starter-data-redis
spring-kafka
redisson-spring-boot-starter
hmdp-repeat-execute-limit-framework
hmdp-service-delay-queue-framework
knife4j-openapi3-jakarta-spring-boot-starter
lombok
hutool-all
jjwt-api
jjwt-impl
jjwt-jackson
spring web client（RestClient）
```

## 十、前端依赖清单

```text
vue
vite
element-plus
axios
pinia
vue-router
echarts
sass
@element-plus/icons-vue
dayjs
```

## 十一、启动顺序

```text
1. 启动本机 MySQL
2. 通过 Docker 启动 Redis
3. 通过 Docker 启动 Kafka
4. 创建 campus_assistant 数据库
5. 执行数据库初始化脚本
6. 检查 application-dev.yml 配置
7. 如需启用 AI，检查 `app.ai.api-key`
8. 启动后端项目
9. 启动前端项目
10. 使用浏览器访问前端页面
```

前端启动：

```bash
npm install
npm run dev
```

后端启动：

```bash
mvn clean package
mvn spring-boot:run
```

## 十二、开发前检查清单

```text
JDK 是否为 17+
Maven 是否可用
Node.js 是否为 18+
MySQL 是否启动
Redis 容器是否启动
Kafka 容器是否启动
数据库 campus_assistant 是否创建
后端数据库账号密码是否正确
前端 API 地址是否正确
Kafka 是否能正常发送和消费消息
Redis 是否可正常读写
如启用 AI，DeepSeek API Key 是否已正确配置
```

## 十三、说明

本项目一期采用单库 MySQL 设计，满足智慧校园助手的业务规模需求。ShardingSphere 分库分表不作为一期必做功能，仅作为后续预约记录、签到记录、操作日志等高增长数据的横向扩展方案预留。
