# 智慧校园助手缓存设计清单

## 1. 设计目标

本项目当前存在以下性能风险：

- 高频查询接口直接访问 MySQL
- 首页数据看板统计查询较重
- 公告、活动、场地等读多写少数据未缓存
- 讨论区、公告详情等接口存在潜在 N+1 查询问题
- Redis 目前主要用于 Token、分布式锁、延迟队列和消息补偿，未承担业务查询缓存职责

本清单用于指导后续缓存层建设，目标是：

- 降低 MySQL 重复读取压力
- 提升高频读接口响应速度
- 为后续 N+1 优化和聚合查询改造提供配套缓存策略
- 保证缓存方案简单、可落地、便于毕设答辩说明

---

## 2. 总体设计原则

### 2.1 缓存优先级

优先缓存以下类型数据：

1. 读多写少的数据  
   如公告列表、活动列表、场地列表。

2. 统计计算开销大的数据  
   如首页数据看板、角色工作台、排行榜。

3. 详情页聚合数据  
   如公告详情、帖子详情。

4. 个性化但可接受短暂不一致的数据  
   如推荐结果、用户工作台摘要。

### 2.2 不优先缓存的数据

以下数据先不做或谨慎做缓存：

1. 强实时库存型数据  
   如场地时间段剩余名额、活动签到瞬时状态。

2. 强事务型写操作结果  
   如预约创建、报名创建、签到提交。

3. 高频变更且强一致要求高的数据  
   如审批中的临界状态。

### 2.3 一致性策略

本项目采用“更新数据库后删除缓存”的基础策略：

1. 先更新 MySQL
2. 再删除相关 Redis Key
3. 下次读取时重新回填缓存

原因：

- 实现简单
- 易于答辩说明
- 对毕设项目足够实用

### 2.4 空值缓存策略

针对详情查询和搜索类接口，需要使用空值缓存防止缓存穿透：

- 查询结果为空时写入短 TTL 空对象
- 推荐 TTL：2 分钟到 5 分钟

### 2.5 Key 命名原则

统一采用：

```text
campus:模块:维度:参数
```

示例：

```text
campus:notice:page:1:10
campus:activity:detail:12
campus:dashboard:stats:ADMIN
```

---

## 3. 缓存对象清单

| 模块 | 接口/对象 | 是否缓存 | Key 示例 | TTL | 失效时机 | 是否允许短暂不一致 |
| --- | --- | --- | --- | --- | --- | --- |
| 用户 | 当前用户基础信息 | 是 | `campus:user:info:{userId}` | 30 分钟 | 用户修改资料、修改密码、管理员改用户 | 是 |
| 首页看板 | 角色统计数据 | 是 | `campus:dashboard:stats:{role}` | 5 分钟 | 预约、报名、签到、公告、讨论等关键写操作后删除 | 是 |
| 首页工作台 | 角色待办/摘要 | 是 | `campus:dashboard:workbench:{role}:{userId}` | 3 分钟 | 用户个人业务变化、审批变化后删除 | 是 |
| 公告 | 公告列表分页 | 是 | `campus:notice:page:{current}:{size}:{role}:{college}` | 10 分钟 | 新增、修改、删除公告；新增公告评论时可不删列表缓存 | 是 |
| 公告 | 公告详情 | 是 | `campus:notice:detail:{noticeId}:{userId}` | 10 分钟 | 修改公告、删除公告、发表评论后删除 | 是 |
| 通知中心 | 通知分页 | 可缓存 | `campus:notification:page:{userId}:{current}:{size}:{readStatus}` | 2 分钟 | 通知新增、已读、全部已读后删除 | 是 |
| 场地 | 场地列表分页 | 是 | `campus:venue:page:{current}:{size}:{status}` | 15 分钟 | 场地新增、修改、删除、状态变更后删除 | 是 |
| 场地 | 场地详情 | 是 | `campus:venue:detail:{venueId}` | 15 分钟 | 场地修改、删除后删除 | 是 |
| 场地时间段 | 时间段列表 | 谨慎缓存 | `campus:venue:slot:{venueId}:{slotDate}` | 1 分钟 | 新增/修改时间段、预约通过、取消预约后删除 | 是 |
| 预约 | 我的预约分页 | 可缓存 | `campus:booking:page:{userId}:{current}:{size}:{status}` | 2 分钟 | 新建预约、审核、取消后删除 | 是 |
| 预约审核 | 教师审核列表 | 可缓存 | `campus:booking:audit:{teacherId}:{current}:{size}:{status}` | 2 分钟 | 新建预约、审核结果变化后删除 | 是 |
| 活动 | 活动列表分页 | 是 | `campus:activity:page:{current}:{size}:{role}:{college}` | 10 分钟 | 活动新增、修改、删除、状态变更后删除 | 是 |
| 活动 | 活动详情 | 是 | `campus:activity:detail:{activityId}:{userId}` | 10 分钟 | 活动修改、删除、报名人数变化后删除 | 是 |
| 活动报名 | 我的报名记录 | 可缓存 | `campus:activity:enroll:{userId}:{current}:{size}` | 2 分钟 | 报名、取消报名后删除 | 是 |
| 活动签到 | 我的签到记录 | 可缓存 | `campus:activity:checkin:{userId}:{current}:{size}` | 2 分钟 | 签到后删除 | 是 |
| 推荐 | 个人推荐结果 | 是 | `campus:recommend:{userId}` | 10 分钟 | 预约、报名、签到、公告变化后删除 | 是 |
| 讨论区 | 帖子列表分页 | 是 | `campus:discussion:page:{current}:{size}:{keyword}` | 5 分钟 | 发帖、删帖、置顶、精华、封禁后删除 | 是 |
| 讨论区 | 帖子详情 | 是 | `campus:discussion:detail:{postId}:{userId}` | 5 分钟 | 评论、删评、点赞、置顶、精华、删帖后删除 | 是 |
| 日志审计 | 日志分页 | 不建议优先缓存 | - | - | - | 否 |
| 系统管理 | 后台用户/公告/场地/活动分页 | 可选 | 后续按模块再加 | 3 分钟 | 后台 CRUD 后删除 | 是 |

---

## 4. 核心模块详细策略

### 4.1 首页数据看板

#### 缓存目标

- 管理员统计卡片
- 教师审批摘要
- 学生个人待办

#### Key 设计

```text
campus:dashboard:stats:{role}
campus:dashboard:workbench:{role}:{userId}
```

#### TTL

- `stats`：5 分钟
- `workbench`：3 分钟

#### 删除时机

- 场地预约创建、审核、取消
- 活动报名、取消报名、签到
- 公告新增删除
- 讨论区发帖删帖

#### 说明

这是第一优先级缓存模块，因为：

- 查询频率高
- SQL 聚合多
- 很适合做答辩中的“性能优化点”

---

### 4.2 公告与通知

#### 公告列表

缓存列表页，减少重复翻页查询。

```text
campus:notice:page:{current}:{size}:{role}:{college}
```

#### 公告详情

缓存公告正文和评论聚合结果。

```text
campus:notice:detail:{noticeId}:{userId}
```

#### 通知中心

用户维度缓存短 TTL。

```text
campus:notification:page:{userId}:{current}:{size}:{readStatus}
```

#### 删除时机

- 发布公告
- 编辑公告
- 删除公告
- 评论公告
- 通知已读/全部已读

---

### 4.3 场地与预约

#### 场地列表

适合长 TTL 缓存：

```text
campus:venue:page:{current}:{size}:{status}
campus:venue:detail:{venueId}
```

#### 场地时间段

只做短 TTL 缓存，不做长缓存。

```text
campus:venue:slot:{venueId}:{slotDate}
```

#### 我的预约/审核列表

只做短 TTL 用户维度缓存：

```text
campus:booking:page:{userId}:{current}:{size}:{status}
campus:booking:audit:{teacherId}:{current}:{size}:{status}
```

#### 删除时机

- 新增预约
- 教师审核通过/驳回
- 用户取消预约
- 场地或时间段修改

#### 说明

预约库存本身不应只依赖缓存判断，仍然必须以数据库事务和 Redisson 锁为准。

---

### 4.4 活动报名与签到

#### 活动列表与详情

```text
campus:activity:page:{current}:{size}:{role}:{college}
campus:activity:detail:{activityId}:{userId}
```

#### 个人报名/签到记录

```text
campus:activity:enroll:{userId}:{current}:{size}
campus:activity:checkin:{userId}:{current}:{size}
```

#### 删除时机

- 发布活动
- 编辑活动
- 删除活动
- 学生报名
- 学生取消报名
- 学生签到

#### 说明

活动人数和签到状态会变化，详情缓存需要及时删除，但仍允许秒级或分钟级短暂不一致。

---

### 4.5 讨论区

#### 帖子列表

```text
campus:discussion:page:{current}:{size}:{keyword}
```

#### 帖子详情

```text
campus:discussion:detail:{postId}:{userId}
```

#### 删除时机

- 发帖
- 删除帖子
- 评论
- 删除评论
- 点赞帖子
- 点赞评论
- 置顶/取消置顶
- 精华/取消精华
- 管理员封禁用户

#### 说明

讨论区适合“缓存 + 批量查询 + 聚合组装”一起优化，单纯上缓存还不够。

---

### 4.6 个性化推荐

#### Key 设计

```text
campus:recommend:{userId}
```

#### TTL

10 分钟

#### 删除时机

- 用户预约成功
- 用户活动报名
- 用户签到
- 公告/活动/场地有明显变化

#### 说明

推荐天然允许短暂不一致，很适合缓存。

---

## 5. 建议新增的缓存基础设施

后续代码实现时，建议统一补这几类基础设施：

### 5.1 Key 常量类

建议新增：

```text
com.campus.assistant.common.cache.CacheKeyConstants
```

作用：

- 统一维护 Redis Key 前缀
- 避免魔法字符串散落在业务代码中

### 5.2 缓存工具类

建议新增：

```text
com.campus.assistant.common.cache.CacheClient
```

建议封装能力：

- 查询缓存
- 写入缓存
- 写入空值缓存
- 删除缓存
- 批量删除缓存

### 5.3 缓存注释日志

建议在热点接口打印：

- 缓存命中
- 缓存未命中
- 回填缓存
- 删除缓存

这样后续答辩时可以拿日志和代码一起解释。

---

## 6. 实施优先级

### 第一阶段：先解决最大性能风险

1. 首页数据看板缓存
2. 公告列表/详情缓存
3. 场地列表/活动列表缓存
4. 推荐结果缓存

### 第二阶段：解决列表和详情的 N+1 问题

1. 讨论区帖子列表
2. 讨论区帖子详情
3. 公告详情评论区
4. 活动报名记录/签到记录

### 第三阶段：补用户维度短缓存

1. 我的预约
2. 我的报名
3. 我的签到
4. 通知中心

---

## 7. 不建议现在做的复杂方案

当前阶段不建议一开始就上：

- 多级缓存
- 本地缓存 + Redis 双层缓存
- Canal / MQ 主动刷新缓存
- 缓存一致性双删重试队列
- 热点缓存永不过期逻辑

原因：

- 对毕设项目实现成本过高
- 代码复杂度上升明显
- 不利于你自己讲清楚

当前项目最适合采用：

`Redis 缓存 + 更新数据库后删除缓存 + 热点接口聚合查询`

---

## 8. 下一步落地顺序

根据这份清单，建议下一步按以下顺序改代码：

1. 新建缓存常量类和缓存工具类
2. 改 `DashboardServiceImpl`
3. 改公告列表和公告详情
4. 改活动列表和场地列表
5. 改推荐模块
6. 再处理讨论区和公告详情的 N+1

---

## 9. 当前结论

本项目后续缓存层建设的重点不是“什么都缓存”，而是：

- 先缓存高频读和统计类接口
- 再消除详情页和列表页的 N+1 查询
- 最后用短 TTL 和删缓存策略保证可接受的一致性

这样改完之后，这个毕设项目就可以较有说服力地回答：

- 为什么用了 Redis
- Redis 不只是存 Token
- 如何缓解数据库压力
- 如何处理缓存一致性
- 如何优化高并发和热点查询场景
