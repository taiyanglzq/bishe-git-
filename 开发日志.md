# 个性化智慧校园助手开发日志

## 第 1 步：项目初始化

目标：

- 在 `bishe` 目录下创建独立的前后端工程。
- 后端工程目录为 `backend`，包名为 `com.campus.assistant`。
- 前端工程目录为 `frontend`，采用 Vue 3 + Vite。
- 原 `hmdp-plus` 项目代码不做修改。

完成内容：

- 创建后端 Maven 工程。
- 创建前端 Vite 工程。
- 创建项目配置文件、基础目录和说明文件。

## 第 2 步：后端基础能力

目标：

- 接入 Spring Boot、MyBatis-Plus、MySQL。
- 统一返回结果。
- 统一异常处理。
- 接口参数校验。
- 登录认证与 JWT。

完成内容：

- 用户登录、退出、当前用户、修改密码。
- 学生默认账号规则：账号为学号，默认密码为身份证后 6 位。
- 教师和管理员账号暂按初始化脚本预置或管理员创建。

## 第 3 步：核心业务模块

目标：

- 实现场地预约、活动报名、签到、公告、推荐、数据看板。
- 处理重复预约、重复报名、重复签到等关键业务规则。

完成内容：

- 同一学生不能重复预约同一场地同一时间段。
- 同一学生不能重复报名同一活动。
- 同一学生不能重复签到同一活动。
- 场地、活动容量不足时不允许继续提交。

## 第 4 步：Redis 与 Redisson 增强

目标：

- 使用 Redis 做缓存、幂等和消息去重。
- 使用 Redisson 实现分布式锁、重复执行限制、延迟队列、布隆过滤器。

完成内容：

- 登录用户缓存。
- 业务重复执行限制。
- 预约、活动、签到提醒延迟任务骨架。

## 第 5 步：Kafka 消息与补偿

目标：

- 使用 Kafka 处理异步通知、日志、缓存失效广播。
- 增加发送失败处理和 DLQ 补偿机制。

完成内容：

- 统一消息体。
- 生产者发送封装。
- 消费者骨架。
- DLQ 消费与补偿记录骨架。

## 第 6 步：前端页面

目标：

- 搭建登录页、首页、公告、预约、活动、签到、推荐、日志、系统管理页面。
- 通过 Axios 对接后端接口。

完成内容：

- 路由守卫。
- Token 存储。
- 基础页面和接口封装。

## 第 7 步：联调与答辩准备

目标：

- 后端可编译启动。
- 前端可运行访问。
- 数据库脚本完整。
- 文档、接口、演示流程完整。

完成内容：

- SQL 初始化脚本。
- 接口清单。
- 启动说明。

## 第 8 步：学生登录后功能完善

目标：

- 学生可正常登录后，继续补齐可展示、可答辩的业务闭环。
- 避免接口返回用户密码等敏感字段。
- 将占位页面升级为可联调页面。

完成内容：

- 新增 `UserVO`，当前用户接口不再返回密码密文、身份证后 6 位等敏感信息。
- 新增个人中心页面，支持查看当前账号、角色、学号和初始密码状态。
- 新增修改密码页面能力，学生可登录后修改默认密码。
- 新增个性化推荐接口和页面，按公告热度、近期活动、可用场地生成推荐结果。
- 新增日志查询接口和日志审计页面，展示预约、审核、取消等操作日志。
- 新增系统管理页面，支持新增用户、发布公告、新增场地、新增活动。
- 公告、场地、活动模块补充新增、修改、删除接口。
- 用户模块补充分页、新增、修改、逻辑删除接口。
- 前端主布局展示当前用户姓名和角色。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 9 步：角色权限与个人业务闭环完善

目标：

- 避免学生看到或操作教师、管理员才有权限的业务。
- 学生只查看自己的预约、报名和签到记录。
- 教师和管理员可以审核预约，学生只能提交和取消自己的预约。
- 日志审计覆盖预约、报名、取消报名和签到。

完成内容：

- 新增 `RoleUtils`，用于后端角色权限判断。
- 预约分页接口按角色过滤：学生只看自己的预约，教师和管理员可查看全部预约。
- 预约分页接口支持按状态筛选：`PENDING`、`APPROVED`、`REJECTED`、`CANCELLED`。
- 预约审核接口限制为教师和管理员角色。
- 前端预约页根据角色显示不同按钮：学生显示取消，教师/管理员显示通过和驳回。
- 新增我的报名接口 `GET /activity/my-enrollments`。
- 新增我的签到接口 `GET /activity/my-checkins`。
- 活动页新增“我的报名”“我的签到”两个标签页。
- 活动报名、取消报名、签到均写入操作日志。
- 前端用户信息持久化到 localStorage，刷新后仍可显示当前用户和角色。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 10 步：预约库存、权限隔离与系统 CRUD 完善

目标：

- 将场地预约从“简单提交”升级为“按日期和时间段控制库存”的真实业务。
- 前端菜单根据角色隔离，减少学生误触管理功能。
- 系统管理从简单表单升级为可查看、可新增、可删除的基础 CRUD 页面。

完成内容：

- 新增场地时间段库存表 `ca_venue_slot`。
- 新增预约时间段实体 `VenueSlot`、Mapper、DTO 和 Controller。
- 新增接口 `GET /venue-slot/page`、`POST /venue-slot`、`PUT /venue-slot`、`DELETE /venue-slot/{id}`。
- 预约提交时根据“场地 + 日期 + 时间段”检查是否开放预约。
- 预约提交时检查剩余名额，名额不足时拒绝提交。
- 预约提交时使用 Redisson 分布式锁保护同一场地同一时间段，降低并发超卖风险。
- 预约提交成功后扣减时间段剩余名额。
- 预约被驳回或取消时释放时间段剩余名额。
- 前端预约页支持选择已开放时间段，并显示剩余名额。
- 前端菜单根据角色过滤：学生不显示系统管理和日志审计，教师/管理员显示管理入口。
- 系统管理页面升级为用户、公告、场地、时间段库存、活动五类基础 CRUD。
- 新增数据库升级脚本 `upgrade-venue-slot.sql`。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 11 步：学生预约页面可用性优化

目标：

- 解决学生预约页没有可预约项目、只能手填场地 ID 的问题。
- 修复预约页面表单横向挤压、按钮溢出、排版不适合真实使用的问题。

完成内容：

- 预约页改为先展示可预约场地卡片。
- 默认预约日期设置为 `2026-06-10`，与初始化时间段库存数据保持一致。
- 选择场地后自动查询该场地当天开放的预约时间段。
- 时间段以卡片形式展示，并显示剩余名额和总名额。
- 名额为 0 的时间段禁用，不允许选择。
- 提交预约前校验场地、日期、时间段和预约原因。
- 预约记录区域与预约提交区域分离。
- 预约审核列表和学生个人预约列表共用页面，但按角色显示不同操作按钮。
- 新增预约页专用响应式布局样式，避免输入框和按钮溢出屏幕。

验证结果：

- 前端 `npm run build` 通过。

## 第 12 步：用户登录号规则严谨化

目标：

- 修正系统管理中同时出现“账号”和“学号”的不严谨设计。
- 统一登录规则：学生使用学号登录，教师使用工号登录，管理员使用账号登录。

完成内容：

- 后端 `UserSaveDTO` 新增 `loginNo` 作为统一登录号。
- 后端保存用户时，统一将 `loginNo` 写入 `username` 字段作为登录凭证。
- 学生用户保存时自动将学号同步到 `studentNo`。
- 教师用户不再填写学号，使用工号作为登录号。
- 管理员用户使用管理员账号作为登录号。
- 前端系统管理用户表单按角色动态展示占位提示：
  - 学生：学号，例如 `23050539414`
  - 教师：工号，例如 `T2026001`
  - 管理员：管理员账号，例如 `admin`
- 用户列表不再同时显示账号和学号，而是显示“学号/工号/账号：登录号”。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 13 步：活动场地关联严谨化

目标：

- 修正活动管理中“地点手动输入”的不严谨设计。
- 活动地点必须来源于场地管理中已维护的场地，保证数据可追溯、可统计、可校验。

完成内容：

- 活动实体新增 `venueId` 字段，用于关联场地。
- 活动保存 DTO 新增必填字段 `venueId`。
- 后端保存活动时，根据 `venueId` 查询场地。
- 如果场地不存在、已删除或已停用，后端拒绝保存活动。
- 活动展示地点由后端根据场地名称和位置生成，格式为 `场地名称（位置）`。
- 前端系统管理的活动地点从手动输入改为场地下拉选择。
- 选择场地后，活动容量默认同步为该场地容量。
- 活动列表新增展示 `venueId`，便于后续统计和追溯。
- 新增数据库升级脚本 `upgrade-activity-venue.sql`。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 14 步：活动时间与签到规则完善

目标：

- 让活动管理更接近真实学校业务，避免活动容量、时间和签到规则随意填写。

完成内容：

- 活动管理新增活动开始时间和结束时间。
- 活动管理新增签到开始时间和签到结束时间。
- 后端校验活动开始时间必须早于活动结束时间。
- 后端校验签到开始时间必须早于签到结束时间。
- 后端校验签到开始时间不能晚于活动开始时间。
- 后端校验活动容量不能超过所选场地容量。
- 学生签到前必须先报名该活动，未报名不能签到。
- 前端活动管理提交时同步提交活动时间范围和签到时间范围。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 15 步：学生端活动中心体验完善

目标：

- 将学生端活动页面从普通表格升级为更适合实际使用的活动中心。
- 明确展示学生对每个活动的报名状态和签到状态。
- 避免学生在未报名、已签到等状态下看到不合理操作。

完成内容：

- 新增 `ActivityVO`，活动列表返回当前登录学生是否已报名、是否已签到。
- 活动列表接口从返回实体改为返回展示 VO。
- 学生端活动页面改为卡片式活动中心。
- 活动卡片展示活动地点、活动时间、签到时间、报名人数和容量。
- 未报名时只显示“报名”按钮。
- 已报名未签到时显示“取消报名”和“签到”按钮。
- 已签到时显示“已完成签到”，不再显示重复签到按钮。
- 活动页面保留“我的报名”和“我的签到”记录页签。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 16 步：通知中心

目标：

- 建立站内通知能力，让预约、审核、活动报名和签到等业务结果能够主动推送给学生。

完成内容：

- 新增通知实体 `Notification`。
- 新增通知表 `ca_notification`。
- 新增通知 Mapper、Service 和 Controller。
- 新增通知分页接口 `GET /notification/page`。
- 新增未读数量接口 `GET /notification/unread-count`。
- 新增单条已读接口 `PUT /notification/read/{id}`。
- 新增全部已读接口 `PUT /notification/read-all`。
- 预约提交成功后生成通知。
- 预约审核通过或驳回后生成通知。
- 学生取消预约后生成通知。
- 活动报名成功后生成通知。
- 取消活动报名后生成通知。
- 活动签到成功后生成通知。
- 前端新增通知中心页面。
- 前端菜单新增通知中心入口。
- 通知中心支持未读/已读筛选、单条标记已读、全部已读。
- 新增数据库升级脚本 `upgrade-notification.sql`。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 17 步：院系级权限与发布范围

目标：

- 按高校常见的校院两级管理模式，区分教师和管理员权限。
- 教师只审核本院系学生预约。
- 公告和活动支持全校发布或本院系发布。

完成内容：

- 用户表新增 `college` 院系字段。
- 公告表新增 `scopeType` 和 `scopeCollege` 字段。
- 活动表新增 `publisherId`、`scopeType` 和 `scopeCollege` 字段。
- 用户管理新增院系填写。
- 学生和教师账号必须维护所属院系，管理员账号可不填院系。
- 教师查看预约列表时，只能看到本院系学生预约。
- 教师审核预约时，后端校验学生院系，不允许审核其他院系学生预约。
- 管理员可查看和审核全部预约。
- 教师发布公告时，只能发布本院系公告。
- 管理员发布公告时，可发布全校公告或指定院系公告。
- 教师发布活动时，只能发布本院系活动。
- 管理员发布活动时，可发布全校活动或指定院系活动。
- 公告列表按范围过滤：普通用户只能看到全校和本院系公告。
- 活动列表按范围过滤：普通用户只能看到全校和本院系活动。
- 新增数据库升级脚本 `upgrade-college-permission.sql`。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 18 步：教师与管理员系统管理权限拆分

目标：

- 修正教师权限和管理员权限过于接近的问题。
- 系统管理按角色显示不同模块，并在后端接口层做硬权限限制。

完成内容：

- 用户管理接口限制为管理员访问。
- 场地新增、修改、删除接口限制为管理员访问。
- 时间段库存新增、修改、删除接口限制为管理员访问。
- 公告新增、修改、删除接口允许教师和管理员访问。
- 活动新增、修改、删除接口允许教师和管理员访问。
- 教师发布公告和活动时，前端固定为本院系范围，不允许选择全校。
- 管理员发布公告和活动时，可以选择全校或指定院系。
- 教师进入系统管理时，只显示公告管理和活动管理。
- 管理员进入系统管理时，显示用户管理、公告管理、场地管理、时间段库存、活动管理。
- 前端避免教师进入系统管理时请求管理员专用接口。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 19 步：教师内容管理范围限制

目标：

- 修正教师在系统管理中可以管理同院系其他教师公告和活动的问题。
- 教师只能管理自己发布的内容，管理员管理全部内容。

完成内容：

- 新增公告管理专用分页接口 `GET /notice/manage/page`。
- 新增活动管理专用分页接口 `GET /activity/manage/page`。
- 教师访问公告管理分页时，只返回自己发布的公告。
- 管理员访问公告管理分页时，返回全部公告。
- 教师访问活动管理分页时，只返回自己发布的活动。
- 管理员访问活动管理分页时，返回全部活动。
- 教师修改公告时，后端校验 `publisherId`，只能修改自己发布的公告。
- 教师删除公告时，后端校验 `publisherId`，只能删除自己发布的公告。
- 教师修改活动时，后端校验 `publisherId`，只能修改自己发布的活动。
- 教师删除活动时，后端校验 `publisherId`，只能删除自己发布的活动。
- 系统管理页面改用管理专用分页接口。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 20 步：系统管理编辑能力完善

目标：

- 将系统管理从“新增/删除”升级为完整基础 CRUD。
- 支持管理员修改场地容量、时间段库存、公告、活动和用户信息。
- 避免场地容量修改破坏已开放时间段库存。

完成内容：

- 用户管理支持编辑回填和更新。
- 公告管理支持编辑回填和更新。
- 场地管理支持编辑回填和更新。
- 时间段库存支持编辑回填和更新。
- 活动管理支持编辑回填和更新。
- 所有管理表单增加“清空”按钮。
- 表格操作区增加“编辑”按钮。
- 场地容量修改时，后端校验已有时间段总名额不能超过新容量。
- 时间段库存表单支持修改剩余名额。
- 前端校验剩余名额不能超过总名额。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。

## 第 21 步：首页工作台与真实数据看板完善

目标：

- 将首页从普通统计页升级为真实校园助手工作台。
- 按学生、教师、管理员不同角色聚合待办事项、近期日程和关键数据。
- 让首页能够直接反映“我现在需要处理什么”，而不是只展示静态数字。

完成内容：

- 新增后端工作台返回对象 `DashboardWorkbenchVO`。
- 新增接口 `GET /dashboard/workbench`，用于返回角色化首页数据。
- 学生首页聚合自己的待审核预约、未读通知、近期可参加活动。
- 教师首页聚合本院系待审核预约、近期活动、未读通知。
- 管理员首页聚合全校待审核预约、系统概览、未读通知。
- 数据看板新增预约状态分布、活动报名排行、热门场地预约量、签到完成率。
- 看板统计改为真实数据库数据，不再使用静态占位内容。
- 活动报名数、签到数统计过滤逻辑删除数据。
- 热门场地预约排行按预约次数降序展示。
- 前端首页改为“工作台 + 指标卡片 + 图表”的组合布局。
- 修复首页、路由、主布局中的中文乱码展示问题。
- 图表增加窗口缩放自适应和页面卸载销毁逻辑，避免重复初始化。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
- 前端 `npm run build` 通过。
- 前端构建存在 Vite 大包体积提示和第三方库注释提示，不影响系统运行。

## 第 22 步：新增 AI 客服助手与内容审核模块

目标：

- 按老师要求为毕设系统增加 AI 能力，体现项目智能化特征。
- 在不大幅侵入原有业务模块的前提下，增加 AI 客服助手和 AI 内容审核员。
- 让讨论区具备基础内容安全能力，并为系统后续接入大模型做好扩展。

完成内容：

- 在后端新增独立 `ai` 包，包含 `config`、`controller`、`dto`、`enums`、`service`、`utils` 等子包。
- 新增 AI 配置类 `AiProperties`，支持 `app.ai.enabled`、`api-key`、`chat-model` 等配置项。
- 新增独立 AI 对话接口 `POST /ai/chat`。
- 新增 `AiChatService`，支持回答系统使用问题和部分个人业务状态问题。
- 接入 DeepSeek `/chat/completions` 接口作为默认 AI 服务端实现。
- 新增 `ContentModerationService`，返回 `PASS`、`FLAG`、`BLOCK` 三类审核结果。
- 在讨论区发帖接口 `POST /discussion/post` 中接入 AI 内容审核。
- 在讨论区评论接口 `POST /discussion/comment` 中接入 AI 内容审核。
- 未配置 AI Key 时，内容审核仍可通过本地敏感词规则完成基础兜底。
- 文档层面补充 AI 客服助手与内容审核员说明，统一更新 README、前后端项目文档、环境与配置文档。

说明：

- 当前阶段优先完成后端 AI 能力接入和配置预留，保证系统能编译、能演示、能扩展。
- 前端聊天入口可在后续继续做成悬浮对话框或侧边栏助手组件。

验证结果：

- 后端 `mvn -q -DskipTests compile` 通过。
## 第 23 步：缓存基础设施与首页数据看板缓存

目标：
- 为后续公告、活动、场地、推荐等热点接口缓存改造建立统一基础设施。
- 优先解决首页数据看板接口每次直连 MySQL、统计查询重复执行的问题。
- 将缓存设计与代码实现同步沉淀进后端结构中，便于后续逐模块扩展。

完成内容：
- 新增统一缓存 Key 常量类 `CacheKeyConstants`。
- 新增统一缓存工具类 `CacheClient`，封装对象缓存、空值缓存、字符串缓存和批量删缓存能力。
- 首页统计接口 `stats()` 接入 Redis 缓存，按角色维度缓存 5 分钟。
- 首页工作台接口 `workbench()` 接入 Redis 缓存，按用户维度缓存 3 分钟。
- 首页概览接口 `summary()` 复用 `stats()` 中的 summary 数据，避免重复统计查询。
- 重构 `DashboardServiceImpl`，统一首页看板、工作台和统计数据的组装逻辑。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。
## 第 24 步：公告列表与详情缓存

目标：
- 降低公告列表和公告详情接口的重复数据库读取压力。
- 解决公告详情读取评论时逐条查询评论用户导致的 N+1 查询问题。
- 在不引入复杂一致性机制的前提下，先建立“更新数据库后删除缓存”的基础缓存策略。

完成内容：
- 公告列表接口 `GET /notice/page` 接入 Redis 缓存，按页码、页大小、角色和院系维度缓存 10 分钟。
- 公告详情接口 `GET /notice/{id}` 接入 Redis 缓存，按公告 ID 和用户维度缓存 10 分钟。
- 新增评论后自动删除对应公告详情缓存。
- 新增、编辑、删除公告后自动删除公告列表缓存和对应详情缓存。
- 优化公告评论查询逻辑，将评论用户信息从逐条 `selectById` 改为批量 `selectBatchIds`，降低 N+1 查询风险。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。
## 第 25 步：场地、活动与推荐模块缓存

目标：
- 继续从高频读接口入手，降低场地列表、活动列表和推荐接口的数据库压力。
- 补齐推荐模块缓存能力，避免推荐页每次都重新查询公告、活动和场地。
- 同步优化活动列表中的热点 N+1 查询问题。

完成内容：
- 场地列表接口 `GET /venue/page` 接入 Redis 缓存，TTL 为 15 分钟。
- 场地新增、编辑、删除后自动删除场地列表缓存，并联动删除推荐缓存。
- 活动列表接口 `ActivityBizServiceImpl.page()` 接入 Redis 缓存，按页码、页大小、角色、院系、用户维度缓存 10 分钟。
- 活动报名、取消报名、签到后自动删除活动列表缓存，并联动删除推荐缓存。
- 活动列表原先按活动逐条查询“是否已报名”“是否已签到”，现已改为批量查询后内存组装，减少热点 N+1。
- 个性化推荐接口 `GET /recommendation/personal` 接入用户维度 Redis 缓存，TTL 为 10 分钟。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。
## 第 26 步：讨论区、活动记录与通知中心性能优化

目标：
- 继续处理剩余高频接口中的 N+1 查询问题。
- 为通知中心补充短 TTL 缓存，降低重复读取未读消息和分页消息列表的数据库压力。
- 对讨论区列表接口做批量查询重构，避免帖子、评论、评论用户、点赞状态逐条查库。

完成内容：
- 通知分页接口 `GET /notification/page` 接入 Redis 短缓存，TTL 为 2 分钟。
- 通知未读数接口 `GET /notification/unread-count` 接入 Redis 短缓存，TTL 为 2 分钟。
- 通知新增、单条已读、全部已读后自动删除通知分页缓存和未读数缓存。
- 活动报名记录 `myEnrollments()` 改为批量查询活动后内存组装，移除逐条查询活动标题的 N+1。
- 活动签到记录 `myCheckins()` 改为批量查询活动后内存组装，移除逐条查询活动标题的 N+1。
- 讨论区分页接口 `GET /discussion/page` 接入 Redis 短缓存，TTL 为 5 分钟。
- 讨论区分页重构为批量查询作者、评论、评论用户、帖子点赞状态、评论点赞状态和评论点赞数，再统一组装 `DiscussionPostVO`。
- 发帖、评论、点赞、删帖、删评、置顶、精华、封禁等讨论区写操作后自动删除讨论区分页缓存。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。
## 第 27 步：缓存失效与业务写操作联动收口

目标：
- 解决删缓存逻辑分散在多个 Controller 和 Service 中、后续容易漏删或乱删的问题。
- 将预约、公告、活动、推荐、通知、讨论区、首页看板的缓存失效逻辑统一收口，便于后续继续扩展。
- 让缓存一致性策略更容易说明、维护和答辩讲解。

完成内容：
- 新增缓存失效服务接口 `CacheEvictService`。
- 新增缓存失效服务实现 `CacheEvictServiceImpl`。
- 将首页看板缓存、推荐缓存、场地缓存、公告缓存、活动缓存、通知缓存、讨论区缓存的删除逻辑统一集中到 `CacheEvictServiceImpl` 中。
- 场地写操作改为调用统一缓存失效服务。
- 公告新增、修改、删除、评论后改为调用统一缓存失效服务。
- 活动报名、取消报名、签到后改为调用统一缓存失效服务。
- 通知新增、已读、全部已读后改为调用统一缓存失效服务。
- 讨论区发帖、评论、点赞、删帖、删评、置顶、精华、封禁后改为调用统一缓存失效服务。
- 将首页看板缓存联动纳入公告、活动、通知、讨论区、场地等关键写操作的缓存清理链路。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。
## 第 28 步：缓存命中日志

目标：
- 为缓存优化提供可观察证据，便于运行时确认是否真正命中缓存。
- 让后续答辩中可以明确说明哪些接口走了 Redis，哪些接口发生了回源和缓存删除。

完成内容：
- 在 `CacheClient` 中统一增加缓存行为日志。
- 新增日志类型：
  - `CACHE_HIT`
  - `CACHE_MISS`
  - `CACHE_HIT_NULL`
  - `CACHE_SET`
  - `CACHE_SET_NULL`
  - `CACHE_DELETE`
  - `CACHE_DELETE_BATCH`
  - `CACHE_DESERIALIZE_FAIL`
- 缓存命中日志统一在缓存工具层输出，不需要每个业务类单独打印。
- 后续只要走 `CacheClient` 的接口，控制台即可直接观察缓存命中情况。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。
## 第 29 步：控制层瘦身与 Service 分层重构

目标：
- 解决部分 Controller 直接持有 Mapper、直接编写业务逻辑、直接操作缓存的分层不清问题。
- 将控制层职责收敛为“接收参数、调用 Service、返回结果”，把核心业务逻辑统一下沉到 `service/impl`。
- 让项目结构更符合 Spring Boot 分层设计规范，也更便于论文答辩时解释系统架构。

完成内容：
- 为讨论区模块新增 `DiscussionService` 与 `DiscussionServiceImpl`，将帖子分页、评论、点赞、封禁、缓存处理等逻辑从 `DiscussionController` 下沉。
- 为公告模块新增 `NoticeService` 与 `NoticeServiceImpl`，将公告详情、评论、权限控制、缓存处理等逻辑从 `NoticeController` 下沉。
- 扩展 `NotificationService`，并在 `NotificationServiceImpl` 中实现通知分页、未读数、已读状态更新逻辑，使 `NotificationController` 回归纯调用层。
- 为场地模块新增 `VenueService` 与 `VenueServiceImpl`，将场地分页、容量校验、缓存联动等逻辑从 `VenueController` 下沉。
- 为场地时间段模块新增 `VenueSlotService` 与 `VenueSlotServiceImpl`，将时间段库存管理逻辑从 `VenueSlotController` 下沉。
- 为推荐模块新增 `RecommendationService` 与 `RecommendationServiceImpl`，将推荐结果聚合与缓存逻辑从 `RecommendationController` 下沉。
- 为用户模块新增 `UserService` 与 `UserServiceImpl`，将用户分页、保存、更新、删除逻辑从 `UserController` 下沉。
- 扩展 `ActivityBizService` 与 `ActivityBizServiceImpl`，将活动管理侧的分页、新增、更新、删除、时间校验、容量校验、院系范围校验统一纳入 Service 层，使 `ActivityController` 只做调用。
- 为日志模块新增 `LogService` 与 `LogServiceImpl`，将日志分页与详情查询逻辑从 `LogController` 下沉。
- 为文件上传模块新增 `FileService` 与 `FileServiceImpl`，将上传校验与文件落盘逻辑从 `FileController` 下沉。

重构结果：
- 现在控制层统一只负责：
  - 参数接收
  - 调用 Service
  - 返回 `Result`
- 业务逻辑、权限判断、缓存操作、数据组装统一收敛到 Service 层。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。

## 第 30 步：图书借阅与归还功能

目标：
- 在图书检索基础上实现借阅和归还完整闭环。
- 通过分布式锁保护借阅/归还并发，防止超借。
- 前端支持一键借阅、一键归还和我的借阅列表。

完成内容：
- 新增 `ca_book_borrow` 表，记录借阅历史（BORROWED/RETURNED 状态）。
- 新增 `BookBorrow` 实体、`BookBorrowMapper`、`BookBorrowDTO`。
- 新增借阅接口 `POST /book/borrow`，使用 Redisson 分布式锁保护。
- 新增归还接口 `POST /book/return/{bookId}`，归还后可用数量加 1。
- 新增我的借阅接口 `GET /book/my-borrows`，查询当前用户借阅记录。
- 借阅校验：图书存在性、上架状态、可借数量、防重复借阅。
- 前端图书详情弹窗智能切换借阅/归还按钮。
- 前端新增"我的借阅"区域展示借阅中/已归还记录。
- 修复前端图书列表数据显示问题（`res.data?.records` → `res.records`）。
- 同步修复课程列表和考试列表的同类数据显示问题。
- 修复 `BookServiceImpl` 缺少 `import java.util.List` 导致编译失败问题。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。

## 第 31 步：AI 多模态交互与双模型架构

目标：
- 实现文本+图片多模态输入能力（第三阶段任务）。
- 解决 DeepSeek 不支持 Vision API 的问题，引入通义千问 VL 模型。
- 前端 AI 助手增加图片上传和预览功能。

完成内容：
- 前端 `AiAssistant.vue` 新增图片上传按钮、缩略图预览、最多4张限制。
- 图片通过 FileReader 转 base64 后调用多模态接口。
- 后端新增独立 `qwenRestClient` Bean，配置通义千问 DashScope 地址。
- `MultimodalClient` 改为对接通义千问 VL 模型（`qwen-vl-plus`）。
- 配置双模型架构：DeepSeek 处理文本，通义千问处理图片。
- `AiProperties` 新增 `qwenApiKey`、`qwenBaseUrl`、`qwenVisionModel` 配置项。
- 修复 DeepSeek API Key 未配置导致 AI 回复兜底消息的问题。
- 修复知识库 RAG 缓存 `LinkedHashMap` 类型转换错误（`CacheClient.getList`）。

验证结果：
- 后端 `mvn -q -DskipTests compile` 通过。

## 第 32 步：校园导航真实数据与当前定位

目标：
- 将校园导航从模拟北京坐标替换为宜春学院真实地理坐标。
- 增加浏览器 GPS 当前定位功能。

完成内容：
- 从用户提供的坐标文件替换 19 个真实建筑位置数据。
- 新增"校门"分类（西门、东门、南门），移除"宿舍楼"分类。
- 新增定位按钮，调用浏览器 Geolocation API 获取当前位置。
- 定位成功后在地图上显示蓝色标记和精度范围圆圈。
- 更新地图中心点为宜春学院图书馆坐标。
- 更新地理编码城市为"宜春"。
- 配置高德地图真实 API Key。

验证结果：
- 地图正常加载宜春学院校园区域，建筑标记和列表显示正确。

## 第 33 步：删除场地预约/场地资源/活动签到模块

目标：
- 按用户要求移除三个不再需要的业务模块，精简系统功能范围。
- 清理所有关联的后端、前端、AI、推荐、看板、缓存、通知代码。

完成内容：
- 删除后端：Booking/Venue/VenueSlot/Activity/ActivityEnroll/Checkin 共 6 个 Entity、6 个 Mapper、4 个 Controller、4 个 Service、6 个 DTO、2 个 VO
- 删除前端：VenueView/BookingView/ActivityView 页面，venue/booking/activity API，对应路由和菜单
- 清理 schema.sql 中 ca_venue/ca_booking/ca_venue_slot/ca_activity/ca_activity_enroll/ca_checkin 表
- 清理 data.sql 中对应 INSERT 语句
- AI 对话删除预约状态/活动状态/预约引导硬编码意图
- 推荐系统移除活动/场地推荐维度，只保留公告/课程/图书推荐
- 首页看板重写：移除预约/活动/签到统计，改为课程/考试数据
- 通知中心/缓存失效/延迟任务中移除关联触发逻辑
- 学习建议构建器移除活动签到统计
- 系统管理页移除场地/时间段库存/活动管理标签页
