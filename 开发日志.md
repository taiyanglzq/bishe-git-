# 个性化智慧校园助手开发日志（完整版）

> 合并自 开发日志.md（第 1-29 步）和 开发日志2.md（第 30-46 步），按时间顺序排列。

## 第 1 步：项目初始化

目标：
- 在 `bishe` 目录下创建独立的前后端工程。
- 后端工程目录为 `backend`，包名为 `com.campus.assistant`。
- 前端工程目录为 `frontend`，采用 Vue 3 + Vite。
- 原 `hmdp-plus` 项目代码不做修改。

完成内容：
- 创建后端 Maven 工程。
- 创建前端 Vite 工程。
- 创建项目配置文件、基础目录和说明文件。

## 第 2 步：后端基础能力

目标：
- 接入 Spring Boot、MyBatis-Plus、MySQL。
- 统一返回结果。
- 统一异常处理。
- 接口参数校验。
- 登录认证与 JWT。

完成内容：
- 用户登录、退出、当前用户、修改密码。
- 学生默认账号规则：账号为学号，默认密码为身份证后 6 位。
- 教师和管理员账号暂按初始化脚本预置或管理员创建。

## 第 3 步：核心业务模块

目标：
- 实现场地预约、活动报名、签到、公告、推荐、数据看板。
- 处理重复预约、重复报名、重复签到等关键业务规则。

完成内容：
- 同一学生不能重复预约同一场地同一时间段。
- 同一学生不能重复报名同一活动。
- 同一学生不能重复签到同一活动。
- 场地、活动容量不足时不允许继续提交。

## 第 4 步：Redis 与 Redisson 增强

目标：
- 使用 Redis 做缓存、幂等和消息去重。
- 使用 Redisson 实现分布式锁、重复执行限制、延迟队列、布隆过滤器。

完成内容：
- 登录用户缓存。
- 业务重复执行限制。
- 预约、活动、签到提醒延迟任务骨架。

## 第 5 步：Kafka 消息与补偿

目标：
- 使用 Kafka 处理异步通知、日志、缓存失效广播。
- 增加发送失败处理和 DLQ 补偿机制。

完成内容：
- 统一消息体。
- 生产者发送封装。
- 消费者骨架。
- DLQ 消费与补偿记录骨架。

## 第 6 步：前端页面

目标：
- 搭建登录页、首页、公告、预约、活动、签到、推荐、日志、系统管理页面。
- 通过 Axios 对接后端接口。

完成内容：
- 路由守卫。
- Token 存储。
- 基础页面和接口封装。

## 第 7 步：联调与答辩准备

目标：
- 后端可编译启动。
- 前端可运行访问。
- 数据库脚本完整。
- 文档、接口、演示流程完整。

完成内容：
- SQL 初始化脚本。
- 接口清单。
- 启动说明。

## 第 8 步：学生登录后功能完善

目标：
- 学生可正常登录后，继续补齐可展示、可答辩的业务闭环。
- 避免接口返回用户密码等敏感字段。
- 将占位页面升级为可联调页面。

完成内容：
- 新增 `UserVO`，当前用户接口不再返回密码密文、身份证后 6 位等敏感信息。
- 新增个人中心页面，支持查看当前账号、角色、学号和初始密码状态。
- 新增修改密码页面能力，学生可登录后修改默认密码。
- 新增个性化推荐接口和页面，按公告热度、近期活动、可用场地生成推荐结果。
- 新增日志查询接口和日志审计页面，展示预约、审核、取消等操作日志。
- 新增系统管理页面，支持新增用户、发布公告、新增场地、新增活动。
- 公告、场地、活动模块补充新增、修改、删除接口。
- 用户模块补充分页、新增、修改、逻辑删除接口。
- 前端主布局展示当前用户姓名和角色。

验证结果：后端编译通过，前端构建通过。

## 第 9 步：角色权限与个人业务闭环完善

目标：
- 避免学生看到或操作教师、管理员才有权限的业务。
- 学生只查看自己的预约、报名和签到记录。
- 教师和管理员可以审核预约，学生只能提交和取消自己的预约。
- 日志审计覆盖预约、报名、取消报名和签到。

完成内容：
- 新增 `RoleUtils`，用于后端角色权限判断。
- 预约分页接口按角色过滤：学生只看自己的预约，教师和管理员可查看全部预约。
- 预约分页接口支持按状态筛选：`PENDING`、`APPROVED`、`REJECTED`、`CANCELLED`。
- 预约审核接口限制为教师和管理员角色。
- 前端预约页根据角色显示不同按钮：学生显示取消，教师/管理员显示通过和驳回。
- 新增我的报名接口 `GET /activity/my-enrollments`。
- 新增我的签到接口 `GET /activity/my-checkins`。
- 活动页新增"我的报名""我的签到"两个标签页。
- 活动报名、取消报名、签到均写入操作日志。
- 前端用户信息持久化到 localStorage，刷新后仍可显示当前用户和角色。

验证结果：后端编译通过，前端构建通过。

## 第 10 步：预约库存、权限隔离与系统 CRUD 完善

目标：
- 将场地预约从"简单提交"升级为"按日期和时间段控制库存"的真实业务。
- 前端菜单根据角色隔离，减少学生误触管理功能。
- 系统管理从简单表单升级为可查看、可新增、可删除的基础 CRUD 页面。

完成内容：
- 新增场地时间段库存表 `ca_venue_slot`。
- 新增预约时间段实体 `VenueSlot`、Mapper、DTO 和 Controller。
- 新增接口 `GET /venue-slot/page`、`POST /venue-slot`、`PUT /venue-slot`、`DELETE /venue-slot/{id}`。
- 预约提交时根据"场地 + 日期 + 时间段"检查是否开放预约。
- 预约提交时检查剩余名额，名额不足时拒绝提交。
- 预约提交时使用 Redisson 分布式锁保护同一场地同一时间段，降低并发超卖风险。
- 预约提交成功后扣减时间段剩余名额。
- 预约被驳回或取消时释放时间段剩余名额。
- 前端预约页支持选择已开放时间段，并显示剩余名额。
- 前端菜单根据角色过滤：学生不显示系统管理和日志审计，教师/管理员显示管理入口。
- 系统管理页面升级为用户、公告、场地、时间段库存、活动五类基础 CRUD。
- 新增数据库升级脚本 `upgrade-venue-slot.sql`。

验证结果：后端编译通过，前端构建通过。

## 第 11 步：学生预约页面可用性优化

目标：
- 解决学生预约页没有可预约项目、只能手填场地 ID 的问题。
- 修复预约页面表单横向挤压、按钮溢出、排版不适合真实使用的问题。

完成内容：
- 预约页改为先展示可预约场地卡片。
- 默认预约日期设置为 `2026-06-10`，与初始化时间段库存数据保持一致。
- 选择场地后自动查询该场地当天开放的预约时间段。
- 时间段以卡片形式展示，并显示剩余名额和总名额。
- 名额为 0 的时间段禁用，不允许选择。
- 提交预约前校验场地、日期、时间段和预约原因。
- 预约记录区域与预约提交区域分离。
- 预约审核列表和学生个人预约列表共用页面，但按角色显示不同操作按钮。
- 新增预约页专用响应式布局样式。

验证结果：前端构建通过。

## 第 12 步：用户登录号规则严谨化

目标：
- 修正系统管理中同时出现"账号"和"学号"的不严谨设计。
- 统一登录规则：学生使用学号登录，教师使用工号登录，管理员使用账号登录。

完成内容：
- 后端 `UserSaveDTO` 新增 `loginNo` 作为统一登录号。
- 后端保存用户时，统一将 `loginNo` 写入 `username` 字段作为登录凭证。
- 前端系统管理用户表单按角色动态展示占位提示。

验证结果：后端编译通过，前端构建通过。

## 第 13 步：活动场地关联严谨化

目标：
- 修正活动管理中"地点手动输入"的不严谨设计。
- 活动地点必须来源于场地管理中已维护的场地。

完成内容：
- 活动实体新增 `venueId` 字段。
- 活动保存 DTO 新增必填字段 `venueId`。
- 后端保存活动时校验场地是否存在。
- 前端系统管理的活动地点从手动输入改为场地下拉选择。

验证结果：后端编译通过，前端构建通过。

## 第 14 步：活动时间与签到规则完善

目标：
- 让活动管理更接近真实学校业务，避免活动容量、时间和签到规则随意填写。

完成内容：
- 活动管理新增活动开始时间和结束时间。
- 活动管理新增签到开始时间和签到结束时间。
- 后端校验活动开始/结束时间、签到时间逻辑。
- 后端校验活动容量不能超过所选场地容量。

验证结果：后端编译通过，前端构建通过。

## 第 15 步：学生端活动中心体验完善

目标：
- 将学生端活动页面从普通表格升级为活动中心。
- 明确展示学生对每个活动的报名状态和签到状态。

完成内容：
- 新增 `ActivityVO`，活动列表返回当前登录学生是否已报名、是否已签到。
- 活动列表接口从返回实体改为返回展示 VO。
- 学生端活动页面改为卡片式活动中心。

验证结果：后端编译通过，前端构建通过。

## 第 16 步：通知中心

目标：
- 建立站内通知能力，让预约、审核、活动报名和签到等业务结果能够主动推送给学生。

完成内容：
- 新增通知实体 `Notification`、表 `ca_notification` 及 Mapper/Service/Controller。
- 新增通知分页、未读数量、单条已读、全部已读接口。
- 预约提交、审核通过/驳回、取消预约、活动报名、取消报名、签到均生成通知。
- 前端新增通知中心页面和菜单入口。

验证结果：后端编译通过，前端构建通过。

## 第 17 步：院系级权限与发布范围

目标：
- 按高校常见的校院两级管理模式，区分教师和管理员权限。
- 教师只审核本院系学生预约，公告和活动支持全校或本院系发布。

完成内容：
- 用户表新增 `college` 院系字段。
- 公告表新增 `scopeType` 和 `scopeCollege` 字段。
- 活动表新增 `publisherId`、`scopeType` 和 `scopeCollege` 字段。
- 教师查看/审核预约时，只能看到本院系学生预约。
- 教师发布公告和活动时，只能发布本院系范围。
- 普通用户只能看到全校和本院系的公告和活动。

验证结果：后端编译通过，前端构建通过。

## 第 18 步：教师与管理员系统管理权限拆分

目标：
- 修正教师权限和管理员权限过于接近的问题。
- 系统管理按角色显示不同模块。

完成内容：
- 用户管理、场地管理、时间段库存接口限制为管理员访问。
- 公告和活动管理允许教师和管理员访问。
- 教师发布公告和活动时固定为本院系范围。
- 系统管理页面按角色动态显示可用模块。

验证结果：后端编译通过，前端构建通过。

## 第 19 步：教师内容管理范围限制

目标：
- 教师只能管理自己发布的内容，管理员管理全部内容。

完成内容：
- 新增公告/活动管理专用分页接口。
- 教师访问管理分页只返回自己发布的内容。
- 教师修改/删除时校验 publisherId。

验证结果：后端编译通过，前端构建通过。

## 第 20 步：系统管理编辑能力完善

目标：
- 将系统管理从"新增/删除"升级为完整基础 CRUD。

完成内容：
- 用户、公告、场地、时间段库存、活动均支持编辑回填和更新。
- 所有管理表单增加"清空"按钮。
- 表格操作区增加"编辑"按钮。

验证结果：后端编译通过，前端构建通过。

## 第 21 步：首页工作台与真实数据看板完善

目标：
- 将首页从普通统计页升级为真实校园助手工作台。
- 按不同角色聚合待办事项、近期日程和关键数据。

完成内容：
- 新增后端工作台返回对象 `DashboardWorkbenchVO`。
- 新增接口 `GET /dashboard/workbench`。
- 学生、教师、管理员分别展示不同维度的首页内容。
- 数据看板使用真实数据库数据。
- 前端首页改为"工作台 + 指标卡片 + 图表"组合布局。

验证结果：后端编译通过，前端构建通过。

## 第 22 步：新增 AI 客服助手与内容审核模块

目标：
- 增加 AI 能力，体现项目智能化特征。
- 在讨论区接入 AI 内容审核。

完成内容：
- 在后端新增独立 `ai` 包（config/controller/dto/enums/service/utils）。
- 新增 AI 配置类 `AiProperties`。
- 新增 AI 对话接口 `POST /ai/chat`，接入 DeepSeek API。
- 新增 `ContentModerationService`，返回 PASS/FLAG/BLOCK。
- 在讨论区发帖和评论接口接入 AI 内容审核。
- 未配置 AI Key 时，通过本地敏感词规则兜底。

验证结果：后端编译通过。

## 第 23 步：缓存基础设施与首页数据看板缓存

目标：
- 为后续缓存改造建立统一基础设施。
- 优先解决首页数据看板接口的数据库压力。

完成内容：
- 新增 `CacheKeyConstants`、`CacheClient`。
- 首页统计接口按角色维度缓存 5 分钟。
- 首页工作台接口按用户维度缓存 3 分钟。

验证结果：后端编译通过。

## 第 24 步：公告列表与详情缓存

目标：
- 降低公告列表和详情接口的数据库压力。
- 解决公告评论 N+1 查询问题。

完成内容：
- 公告列表和详情接口接入 Redis 缓存。
- 优化公告评论查询，改为批量查询用户。

验证结果：后端编译通过。

## 第 25 步：场地、活动与推荐模块缓存

目标：
- 降低场地列表、活动列表和推荐接口的数据库压力。
- 优化活动列表中的 N+1 查询问题。

完成内容：
- 场地列表、活动列表、个性化推荐接口接入 Redis 缓存。
- 活动列表改为批量查询后内存组装。

验证结果：后端编译通过。

## 第 26 步：讨论区、活动记录与通知中心性能优化

目标：
- 处理剩余高频接口中的 N+1 查询问题。
- 为通知中心补充短 TTL 缓存。

完成内容：
- 通知分页和未读数接口接入 Redis 短缓存。
- 活动报名记录和签到记录改为批量查询。
- 讨论区分页接口接入 Redis 缓存，重构为批量查询。

验证结果：后端编译通过。

## 第 27 步：缓存失效与业务写操作联动收口

目标：
- 将分散的删缓存逻辑统一收口。

完成内容：
- 新增 `CacheEvictService` / `CacheEvictServiceImpl`。
- 将首页、推荐、场地、公告、活动、通知、讨论区的缓存失效逻辑统一集中。

验证结果：后端编译通过。

## 第 28 步：缓存命中日志

目标：
- 为缓存优化提供可观察证据。

完成内容：
- 在 `CacheClient` 中统一增加缓存行为日志（HIT/MISS/SET/DELETE 等）。

验证结果：后端编译通过。

## 第 29 步：控制层瘦身与 Service 分层重构

目标：
- 将业务逻辑从 Controller 下沉到 Service 层。

完成内容：
- 为讨论区、公告、通知、场地、时间段、推荐、用户、活动、日志、文件模块新增或扩展 Service 层。
- 控制器统一只负责参数接收、调用 Service、返回 Result。

验证结果：后端编译通过。

---

## 第 30 步：教务系统模拟数据设计

目标：
- 为课程查询和考试提醒功能建立数据基础。

完成内容：
- 新增 `ca_course` 表、`ca_exam` 表及对应实体、Mapper、DTO、Service、Controller。
- 预置 7 门课程和 8 条考试数据。
- 教师只能管理本院系课程和考试，管理员可管理全部。

验证结果：后端编译通过。

## 第 31 步：课程查询功能

目标：
- 为学生、教师和管理员提供课程查询和考试安排查看能力。

完成内容：
- 新增前端 `CourseView.vue`，卡片网格展示，支持院系/学期/关键字筛选。
- 点击可查看课程详情和关联考试安排。

验证结果：后端编译通过，前端构建通过。

## 第 32 步：考试提醒功能

目标：
- 提供考试安排查看页面，考试创建后自动推送通知和考前提醒。

完成内容：
- 后端考试创建后向同院系学生推送通知，Redisson 延迟队列调度考前提醒。
- 新增前端 `ExamView.vue`，列表卡片展示，未来 3 天考试高亮和倒计时。

验证结果：后端编译通过，前端构建通过。

## 第 33 步：图书检索功能

目标：
- 提供馆藏图书检索能力，展示馆藏位置和借阅状态。

完成内容：
- 新增 `ca_book` 表及对应实体、Mapper、DTO、Service、Controller。
- 预置 10 本模拟图书，覆盖 4 个分类。
- 新增前端 `BookView.vue`，卡片网格 + 分类/关键字搜索 + 详情弹窗。

验证结果：后端编译通过，前端构建通过。

## 第 34 步：校园导航功能

目标：
- 使用高德地图展示校园建筑位置，提供搜索和路径指引。

完成内容：
- 新增前端 `CampusMapView.vue`，集成高德地图 JS API v2.0。
- 预置 12 个校园建筑标注，支持分类筛选和自动补全搜索。

验证结果：后端编译通过，前端构建通过。

## 第 35 步：校园知识库与 RAG 检索增强

目标：
- 构建校园知识库，为 AI 对话提供 RAG 检索增强。

完成内容：
- 新增 `ca_knowledge` 表及对应实体、Mapper、DTO、Service、Controller。
- 预置 15 条知识条目，关键词匹配打分 + Redis 缓存。
- `AiChatServiceImpl` 集成 RAG，检索结果作为 DeepSeek prompt 上下文。

验证结果：后端编译通过。

## 第 36 步：多轮对话引擎

目标：
- 将 AI 对话升级为多轮，支持上下文记忆和会话管理。

完成内容：
- 新增 `ConversationService`，基于 Redis 管理会话和历史消息。
- 重构 `AiChatService` 接口，支持 `chat(AiChatRequest, User)`。
- `AiChatController` 新增会话列表/历史/删除端点。
- 前端 `AiAssistant.vue` 升级为多轮聊天界面。

验证结果：后端编译通过，前端构建通过。

## 第 37 步：个性化学习建议与学习资源推荐

目标：
- 基于学生数据提供 AI 个性化学习建议，扩展推荐系统。

完成内容：
- 新增 `LearningAdviceBuilder`，新增 `POST /ai/learning-advice` 端点。
- 扩展 `RecommendationServiceImpl`，新增课程、图书、知识库推荐维度。

验证结果：后端编译通过。

## 第 38 步：多模态交互（初版）

目标：
- 支持文本+图片的多模态输入，对接 DeepSeek Vision API。

完成内容：
- 新增 `MultimodalClient`，支持 base64 图片输入，兼容 OpenAI Vision API 格式。
- 新增 `POST /ai/chat/multimodal` 端点。
- 新增 `MultimodalChatRequest` DTO。
- 前端 `ai.js` 新增 `chatWithImage()` 和 `getLearningAdvice()` 方法。

验证结果：后端编译通过，前端构建通过。

---

## 第 39 步：图书借阅与归还功能

目标：
- 在图书检索基础上实现借阅和归还完整闭环。

完成内容：
- 新增 `ca_book_borrow` 表、BookBorrow 实体/Mapper/DTO。
- 借阅接口 `POST /book/borrow` + 归还 `POST /book/return/{bookId}` + 我的借阅 `GET /book/my-borrows`。
- Redisson 分布式锁保护并发，唯一索引防重复借阅。
- 前端详情弹窗智能切换借阅/归还按钮，新增"我的借阅"区域。
- 修复 `res.data?.records` → `res.records` 数据显示问题。

验证结果：后端编译通过。

## 第 40 步：公告审核流程与通知

目标：
- 教师发布公告后需审核，审核通过/驳回后通知发布者。

完成内容：
- 教师发布公告默认 status=0（待审核），管理员发布直接通过。
- AI 自动审核：PASS → 自动通过，FLAG → 待人工审核，BLOCK → 自动驳回。
- 管理员可通过/驳回公告。
- 审核结果通过通知中心推送给发布者。

验证结果：后端编译通过。

## 第 41 步：AI 多模态修复与双模型架构

目标：
- 解决 DeepSeek 不支持 Vision API 的问题。

完成内容：
- 前端 AiAssistant.vue 新增图片上传、缩略图预览、发送功能。
- 后端双模型架构：DeepSeek 处理文本，通义千问 VL 处理图片。
- 修复 DeepSeek API Key 未配置导致兜底消息的问题。
- 修复知识库 RAG 缓存 LinkedHashMap 类型转换错误。

验证结果：后端编译通过。

## 第 42 步：校园导航真实数据与当前定位

目标：
- 替换宜春学院真实坐标，增加 GPS 定位功能。

完成内容：
- 替换 19 个宜春学院真实建筑坐标数据。
- 新增"校门"分类（西门/东门/南门）。
- 新增定位按钮（浏览器 Geolocation API），蓝色标记 + 精度圆圈。
- 更新地图中心点和地理编码城市。

验证结果：前端联调通过。

## 第 43 步：删除场地预约/场地资源/活动签到模块

目标：
- 按用户要求移除三个业务模块及所有关联代码。

完成内容：
- 删除 30+ 后端文件（Controller/Service/Entity/Mapper/DTO/VO）。
- 删除 6 个前端文件（页面 + API）。
- 清理 SQL 中 6 张表。
- 清理 AI 对话、推荐系统、首页看板、缓存失效中的关联逻辑。
- 修复 MyBatis-Plus 软删除问题。

验证结果：后端编译通过。

## 第 44 步：管理员图书管理

目标：
- 管理员可在系统管理页新增/编辑/删除图书，支持上传封面。

完成内容：
- SystemView 新增图书管理标签页（仅 ADMIN）。
- 支持封面上传、分类选择、库存管理。
- 后台权限改为仅 ADMIN（原为 TEACHER/ADMIN）。
- 新增借阅记录标签页，支持搜索书名/作者/ISBN。

验证结果：后端编译通过。

## 第 45 步：考试监考老师与座位管理

目标：
- 考试增加监考老师字段，支持座位生成/编辑/导出和学生端展示。

完成内容：
- Exam 实体新增 invigilator 字段。
- 新增 `ca_exam_seat` 表，支持按教室行列/学号/随机三种模式生成座位。
- 生成预览 → 手动保存 → 导出 Excel（Apache POI）。
- 学生考试页面显示个人座位号（从 ca_exam_seat 表读取）。
- 系统管理新增考试管理标签页（仅 ADMIN）。

验证结果：后端编译通过。

## 第 46 步：批量导入学生账号

目标：
- 导入 40 个计算机学院学生账号，用于测试座位分配和登录。

完成内容：
- 新增 `upgrade-students.sql`，学号 23050539401~23050539440。
- 默认密码 123456（BCrypt 加密），院系为计算机学院。
- 已有账号 23050529414 保持不变。

验证结果：SQL 脚本已提交。
