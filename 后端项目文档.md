# 个性化智慧校园助手后端项目文档

## 一、项目概述

### 1.1 项目名称

`campus-assistant-backend`

### 1.2 项目定位

本项目是“基于 Spring Boot + Vue 的个性化智慧校园助手的设计与实现”的后端工程，负责业务接口、用户认证、角色权限、数据库访问、缓存控制、Kafka 异步消息、Redisson 并发控制、日志记录、统计分析以及 AI 客服与内容审核能力。

后端不包含前端页面，只向前端提供 RESTful API。

## 二、后端技术栈

| 技术栈 | 应用场景 |
| --- | --- |
| Spring Boot 3.x | 后端主框架 |
| Spring MVC | RESTful API |
| MyBatis-Plus | 数据库 CRUD、分页、条件查询、逻辑删除 |
| MySQL 8.x | 业务数据存储 |
| Redis | Token 缓存、验证码、热点数据、限流、消息去重 |
| Redisson | 分布式锁、缓存重建互斥、布隆过滤器、延迟队列、重复执行限制 |
| Kafka | 异步通知、消息解耦、日志异步处理、DLQ 补偿 |
| Bean Validation | 接口参数校验 |
| JWT | 无状态登录认证 |
| DeepSeek API | AI 文本对话、内容审核 |
| 通义千问 API | AI 多模态图片识别 |
| Knife4j / Swagger | 接口文档 |
| Lombok | 简化实体类和 DTO 编写 |
| Hutool | 通用工具处理 |

## 三、后端项目结构

```text
backend
└─ src
   └─ main
      ├─ java
      │  └─ com
      │     └─ campus
      │        └─ assistant
      │           ├─ CampusAssistantApplication.java
      │           ├─ ai
      │           │  ├─ config
      │           │  ├─ controller
      │           │  ├─ dto
      │           │  ├─ enums
      │           │  ├─ service
      │           │  │  └─ impl
      │           │  └─ utils
      │           ├─ common
      │           │  ├─ config
      │           │  ├─ exception
      │           │  ├─ interceptor
      │           │  ├─ result
      │           │  └─ utils
      │           ├─ controller
      │           ├─ dto
      │           ├─ entity
      │           ├─ mapper
      │           ├─ service
      │           │  └─ impl
      │           ├─ vo
      │           ├─ kafka
      │           │  ├─ producer
      │           │  ├─ consumer
      │           │  ├─ dlq
      │           │  └─ message
      │           ├─ config
      │           └─ util
      └─ resources
         ├─ application.yml
         ├─ application-dev.yml
         ├─ mapper
         └─ sql
```

## 四、后端分层设计

| 层级 | 职责 |
| --- | --- |
| Controller | 接收请求、参数校验、返回统一结果 |
| Service | 编写业务逻辑 |
| Mapper | 访问数据库 |
| Entity | 数据库表映射 |
| DTO | 接收请求参数 |
| VO | 返回前端展示数据 |
| Interceptor | 登录校验、Token 解析、权限判断 |
| Exception | 统一异常处理 |
| Kafka Producer | 发送异步消息 |
| Kafka Consumer | 消费异步消息 |
| Kafka DLQ | 处理死信消息、补偿和重放 |
| AI Service | 封装 AI 客服和内容审核逻辑 |

## 五、核心业务模块

### 5.1 认证模块

功能：

- 用户登录
- 生成 JWT
- 解析 JWT
- 查询当前登录用户
- 退出登录
- 修改密码

接口：

```text
POST /auth/login
POST /auth/logout
GET  /user/current
PUT  /user/password
```

说明：

- 学生账号默认为学号，例如 `23050539414`，初始密码默认为身份证后 6 位。
- 学生首次登录后可以修改密码，系统应保存加密后的密码密文，不允许明文保存身份证后 6 位。
- 教师账号初期暂按“初始化脚本预置或管理员后台创建”处理。
- 管理员账号初期暂按“初始化脚本预置超级管理员，再由管理员创建其他管理员”处理。
- 系统不开放前台自由注册，避免校园身份数据失控。

### 5.2 用户与角色模块

功能：

- 用户分页查询
- 用户新增、修改、禁用、删除
- 角色管理
- 用户角色分配
- 学生初始账号维护
- 教师和管理员账号维护

相关表：

```text
ca_user
ca_role
ca_user_role
```

### 5.3 公告模块

功能：

- 公告分页查询
- 公告详情
- 公告新增、修改、删除、发布、下架
- 热点公告缓存

接口：

```text
GET    /notice/page
GET    /notice/{id}
POST   /notice
PUT    /notice
DELETE /notice/{id}
```

### 5.4 场地预约模块

功能：

- 场地预约
- 预约审核
- 预约取消
- 预约状态查询
- 同一学生、同一场地、同一时间段防重复预约

接口：

```text
GET  /booking/page
POST /booking
PUT  /booking/approve
PUT  /booking/reject
POST /booking/cancel
```

业务规则：

- 同一学生不能重复预约同一场地的同一时间段。
- 场地可用名额不足时，预约申请不能创建成功。
- 已通过审核的预约被取消后，应释放对应时间段的可用名额。
- 审核通过、审核驳回、取消预约均需要记录操作日志。

### 5.5 活动报名模块

功能：

- 活动报名
- 报名取消
- 报名名单查询
- 参与统计

业务规则：

- 同一学生不能重复报名同一活动。
- 活动报名人数不能超过活动容量上限。
- 取消报名后应释放活动剩余名额。

### 5.6 签到模块

功能：

- 活动签到
- 签到记录查询
- 防重复签到

业务规则：

- 同一学生不能重复签到同一活动。
- 签到应限制在活动允许的签到时间范围内。
- 重复签到请求应返回明确提示，不新增签到记录。

### 5.7 个性化推荐模块

功能：

- 根据用户角色推荐公告
- 根据浏览、预约、报名、签到行为推荐活动
- 根据历史预约记录推荐场地
- 缓存推荐结果

接口：

```text
GET /recommendation/personal
```

### 5.8 数据看板模块

功能：

- 用户数量统计
- 公告数量统计
- 预约数量统计
- 签到完成率统计
- 活动参与统计
- 热点数据统计

接口：

```text
GET /dashboard/summary
GET /dashboard/trend
GET /dashboard/booking-stat
```

### 5.9 日志管理模块

功能：

- 登录日志
- 操作日志
- Kafka 消息日志
- 审核日志
- DLQ 补偿日志

接口：

```text
GET /log/page
GET /log/{id}
```

### 5.10 AI 客服助手模块

功能：

- 回答系统使用问题
- 回答校园助手常见高频问题
- 基于当前登录用户上下文回答部分个人业务问题
- 输出模块入口建议，例如引导用户前往预约页或通知中心

接口：

```text
POST /ai/chat
```

说明：

- AI 客服助手采用独立 `ai` 包实现，不侵入原有业务模块结构。
- 当前默认对接 DeepSeek `/chat/completions` 接口。
- 当未配置 AI Key 时，系统可返回兜底说明，不影响主系统运行。

### 5.11 AI 内容审核模块

功能：

- 审核讨论区帖子内容
- 审核讨论区评论内容
- 基于规则和大模型返回 `PASS`、`FLAG`、`BLOCK`
- 对明显违规内容进行拦截

当前集成点：

- `POST /discussion/post`
- `POST /discussion/comment`

审核策略：

- `PASS`：允许发布
- `FLAG`：允许发布或提示用户优化措辞
- `BLOCK`：拒绝发布并返回违规提示

说明：

- 当前项目已在讨论区发帖和评论流程接入内容审核服务。
- 未配置外部 AI 模型时，仍可通过本地敏感词规则完成基础审核兜底。

### 5.12 课程管理模块

功能：

- 课程分页查询
- 课程详情
- 按院系、学期、关键字筛选
- 课程新增、修改、删除
- 管理端课程分页

接口：

```text
GET  /course/page
GET  /course/{id}
GET  /course/manage/page
POST /course
PUT  /course
DELETE /course/{id}
```

业务规则：

- 教师只能管理本院系课程，管理员可管理全部课程。
- 课程支持按院系、学期和关键字（课程名称/教师）搜索。
- 预置模拟数据覆盖计算机学院、外国语学院、数学学院。

### 5.13 考试管理模块

功能：

- 考试分页查询
- 考试详情
- 按院系、考试类型、关键字筛选
- 考试新增、修改、删除
- 考试创建后自动推送通知（含监考老师信息）
- 考前 Redisson 延迟队列提醒
- **监考老师**：Exam 实体新增 `invigilator` 字段
- **座位管理**：新增 `ca_exam_seat` 表，按学生维度分配座位
- **座位生成**：支持按教室行列（A1,B1...）/ 学号排序 / 随机三种模式
- **座位导出**：Apache POI 导出 Excel（含序号/学号/姓名/院系/座位号）
- **学生端展示**：`GET /exam/seats/my` 返回当前学生所有座位

接口：

```text
GET    /exam/page
GET    /exam/{id}
GET    /exam/manage/page
POST   /exam
PUT    /exam
DELETE /exam/{id}
POST   /exam/{id}/seats/generate    生成座位预览
PUT    /exam/{id}/seats/save         保存座位
GET    /exam/{id}/seats              查询已保存座位
PUT    /exam/seats/{seatId}          修改单个座位号
GET    /exam/{id}/seats/export       导出座位表 Excel
GET    /exam/seats/my                当前学生座位（学生端）
```

业务规则：

- 考试结束时间必须晚于开始时间。
- 新增考试后自动向同院系所有学生发送通知。
- 考前 1 天自动投递 Redisson 延迟队列提醒任务。
- 教师只能管理本院系考试，管理员可管理全部。
- 座位生成是预览模式，需手动保存后才写入数据库。
- 座位导出只能导出已保存的数据。

### 5.14 图书检索与借阅模块

功能：

- 图书分页检索
- 图书详情
- 按分类和关键字（书名/作者/ISBN）搜索
- 图书新增、修改、删除
- 展示馆藏位置和借阅状态
- **借阅图书**：用户借阅后可用数量减 1，创建借阅记录
- **归还图书**：用户归还后可用数量加 1，更新借阅记录状态
- **我的借阅**：查询当前用户的借阅记录

接口：

```text
GET  /book/page
GET  /book/{id}
GET  /book/manage/page
POST /book
PUT  /book
DELETE /book/{id}
POST /book/borrow
POST /book/return/{bookId}
GET  /book/my-borrows
```

业务规则：

- 图书支持按分类筛选和关键字搜索。
- 展示总册数和可借册数，可借册数为 0 时标记"已借完"。
- 预置 10 本模拟图书，覆盖计算机科学、数学、外语、文学四个分类。
- 借阅/归还使用 Redisson 分布式锁保护并发（锁 Key：`ca:lock:borrow:{bookId}`）。
- 同一用户不能重复借阅同一图书（唯一索引 + 业务校验）。
- 已下架（status=0）或已删除的图书不允许借阅。
- 新增 `ca_book_borrow` 表记录借阅历史，支持 BORROWED/RETURNED 状态。

### 5.15 知识库管理模块

功能：

- 知识条目分页查询
- 知识条目详情
- 按分类和关键字搜索
- 知识条目新增、修改、删除
- RAG 检索增强：关键词匹配打分，返回 Top-N 结果

接口：

```text
GET  /knowledge/page
GET  /knowledge/{id}
GET  /knowledge/manage/page
POST /knowledge
PUT  /knowledge
DELETE /knowledge/{id}
```

业务规则：

- 知识库全量缓存到 Redis（TTL 30 分钟），避免每次检索全表扫描。
- RAG 检索流程：精确匹配 +10 分，关键词匹配 +5 分，词匹配累加。
- 预置 15 条知识条目，覆盖校园规章、学习指导、生活服务。

### 5.16 AI 多轮对话、双模型架构与会话管理

功能：

- 多轮对话：上下文历史拼接
- 会话管理：创建、列表、切换、删除
- 对话历史 Redis 存储（最近 20 条，TTL 24 小时）
- **双模型架构**：DeepSeek 处理文本对话，通义千问 VL 处理多模态图片识别
- 知识库 RAG 检索增强（关键词匹配打分 + Redis 缓存）
- 个性化学习建议生成

接口：

```text
POST   /ai/chat
GET    /ai/sessions
GET    /ai/sessions/{sessionId}/history
DELETE /ai/sessions/{sessionId}
POST   /ai/learning-advice
POST   /ai/chat/multimodal
```

说明：

- 前端传入 sessionId 可关联历史对话，不传则自动创建新会话。
- 响应中包含 sessionId 供前端保存。
- `POST /ai/learning-advice` 基于学生课程、考试和活动数据生成个性化学习建议。
- `POST /ai/chat/multimodal` 支持文本+图片多模态输入，后端路由至通义千问 VL 模型（`qwen-vl-plus`）。
- **双模型架构**：`DeepSeekClient` 对接 DeepSeek API 处理纯文本对话，`MultimodalClient` 对接通义千问 DashScope API 处理图片识别。两个 Client 使用独立的 `RestClient` Bean（`aiRestClient` 和 `qwenRestClient`）。
- 知识库 RAG 检索使用 `CacheClient.getList()` 方法解决泛型擦除导致的 `LinkedHashMap` 类型转换问题。

### 5.17 Redisson 增强模块

Redisson 用于解决并发和异步任务问题：

- 分布式锁
- 缓存重建互斥
- 布隆过滤器
- 重复执行限制
- 延迟队列

重复执行限制适用接口：

```text
POST /booking
POST /checkin
POST /activity/enroll
PUT  /booking/approve
PUT  /booking/reject
```

延迟队列适用场景：

```text
预约开始前提醒
活动开始前提醒
签到开始提醒
预约超时自动取消
缓存延迟删除
失败消息延迟补偿
```

延迟任务消息体示例：

```json
{
  "taskId": 1,
  "taskType": "BOOKING_REMIND",
  "bizId": 1001,
  "userId": 1,
  "executeTime": "2026-06-01 10:00:00"
}
```

### 5.13 Kafka 消息模块

Kafka 主要用于：

- 预约成功通知
- 预约审核结果通知
- 活动开始提醒
- 公告发布通知
- 日志异步落库
- 缓存失效广播
- 消息发送失败处理
- DLQ 死信队列补偿
- 消费异常重放
- 失败消息审计

失败补偿机制：

- 生产者发送失败后记录失败日志并投递 DLQ。
- 消费者处理异常后触发补偿流程。
- DLQ 消费者负责消息重放和人工排查。
- 失败与补偿结果统一落库，便于后期审计。

## 六、数据库实体设计

核心实体：

```text
User
Role
Notice
NoticeComment
Venue
VenueSlot
Activity
ActivityEnroll
Booking
CheckIn
Course
Exam
ExamSeat
Book
BookBorrow
KnowledgeEntry
Notification
Recommendation
OperationLog
LoginLog
KafkaMessageLog
DelayTask
DiscussionPost
DiscussionComment
DiscussionLike
DiscussionCommentLike
DiscussionUserBan
```

设计原则：

- 主键统一使用 `id`
- 时间字段统一使用 `createTime`、`updateTime`
- 状态字段使用枚举表示
- 逻辑删除字段统一使用 `deleted`
- 不依赖数据库物理外键，使用业务字段进行逻辑关联

## 七、统一返回规范

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

常见状态码：

```text
200 成功
400 参数错误
401 未登录或 Token 失效
403 无权限
404 数据不存在
409 业务冲突，例如重复预约、重复签到
500 服务异常
```

## 八、权限设计

| 角色 | 权限 |
| --- | --- |
| 学生 | 查看公告、预约场地、报名活动、签到、查看个人推荐 |
| 教师 | 审核预约、发布公告、查看统计 |
| 管理员 | 用户管理、系统管理、日志审计、全局数据看板 |

权限实现方式：

- 登录后签发 JWT
- 前端携带 Token 请求接口
- 后端拦截器解析 Token
- 根据 `roleCode` 进行权限判断
- 管理类接口必须在后端再次校验权限

## 九、业务亮点设计

### 9.1 个性化推荐

根据用户角色、浏览行为、预约行为、活动报名和签到行为，推荐更相关的公告、活动和场地。

### 9.2 Redis 与 Redisson 幂等控制

通过 Redis 和 Redisson 控制重复预约、重复签到、重复报名、重复审核和重复消息消费。

### 9.3 Kafka 异步通知与 DLQ 补偿

通过 Kafka 解耦通知、日志、缓存刷新和补偿流程；通过 DLQ 处理发送失败、消费异常和补偿重放。

### 9.4 AI 客服与内容审核

AI 客服助手作为系统前台智能问答入口，独立提供自然语言问答服务；AI 内容审核员作为讨论区后台安全保障能力，审核帖子和评论内容，降低教师和管理员的人工审查压力。

### 9.5 数据看板

聚合展示预约量、签到率、公告热度、活动参与度和用户活跃度。

## 十、开发顺序建议

```text
1. 初始化 Spring Boot 项目
2. 接入 MySQL 与 MyBatis-Plus
3. 完成统一返回和异常处理
4. 完成登录认证和权限拦截
5. 完成用户和角色管理
6. 完成公告模块
7. 完成预约、活动和签到模块
8. 完成 Redis 缓存与 Redisson 幂等控制
9. 完成 Redisson 延迟队列
10. 完成 Kafka 消息与 DLQ 补偿
11. 完成推荐和统计模块
12. 完成 AI 客服助手与内容审核模块
13. 完成日志模块
14. 完成前后端联调
```

## 十一、说明

后端一期采用单库 MySQL 设计，不强制使用 ShardingSphere 分库分表。分库分表仅作为后续高增长数据表的扩展方案，避免在毕设一期引入不必要的复杂度。
### 9.6 缓存基础设施与首页看板缓存

为解决首页高频查询直接访问 MySQL 的问题，后端新增了统一缓存层基础设施：

- `CacheKeyConstants`
  用于统一维护 Redis 业务缓存前缀，避免各模块散落硬编码 Key。
- `CacheClient`
  用于统一封装对象缓存、字符串缓存、空值缓存、单条删除和批量删除操作。

当前已优先在首页数据看板模块落地缓存：

- `GET /dashboard/stats`
  按角色维度缓存首页统计数据，TTL 为 5 分钟。
- `GET /dashboard/workbench`
  按用户维度缓存工作台数据，TTL 为 3 分钟。
- `GET /dashboard/summary`
  复用 `stats` 中的 summary 结果，不再单独重复查询数据库。

当前缓存策略说明：

- 采用“查询未命中再回填”的读缓存模式。
- 采用“短 TTL + 后续业务写操作删缓存”的一致性策略。
- 当前阶段优先解决高频读和统计类接口，不直接用缓存替代预约库存等强一致业务判断。
### 9.7 公告模块缓存优化

公告模块已完成第一批业务缓存落地，主要包括：

- `GET /notice/page`
  按 `current + size + role + college` 维度缓存公告分页结果，TTL 为 10 分钟。
- `GET /notice/{id}`
  按 `noticeId + userId` 维度缓存公告详情结果，TTL 为 10 分钟。

缓存失效策略：

- 发布公告后删除公告列表缓存。
- 修改公告后删除公告列表缓存和当前公告详情缓存。
- 删除公告后删除公告列表缓存和当前公告详情缓存。
- 新增公告评论后删除当前公告详情缓存。

公告详情性能优化：

- 原公告评论查询存在“每条评论单独查询一次评论用户”的 N+1 问题。
- 当前已改为先批量查询评论，再批量查询评论用户，最后在内存中组装 `NoticeCommentVO`。

当前收益：

- 公告列表和公告详情从“每次请求都直连 MySQL”转为“优先走 Redis 缓存”。
- 公告详情评论区的数据库访问次数明显下降。
### 9.8 场地、活动与推荐缓存优化

在首页看板和公告模块之后，后端继续对场地、活动和推荐模块进行了缓存优化：

#### 场地列表缓存

- `GET /venue/page`
  已按分页维度缓存场地列表结果，TTL 为 15 分钟。
- 场地新增、修改、删除后自动删除场地列表缓存。

#### 活动列表缓存

- 活动列表读取逻辑位于 `ActivityBizServiceImpl.page()`。
- 当前已按 `current + size + role + college + userId` 维度缓存活动列表结果，TTL 为 10 分钟。
- 学生报名、取消报名、活动签到后自动删除活动列表缓存。

#### 活动列表性能优化

活动列表原先存在按活动逐条查询当前用户报名状态和签到状态的问题。当前已优化为：

- 先批量取出当前页活动 ID
- 批量查询当前用户在这些活动中的报名记录
- 批量查询当前用户在这些活动中的签到记录
- 最终在内存中组装 `ActivityVO`

这样可显著降低活动列表接口的数据库访问次数。

#### 推荐结果缓存

- `GET /recommendation/personal`
  已按用户 ID 维度缓存推荐结果，TTL 为 10 分钟。
- 当场地、活动等核心推荐源数据发生变化时，会同步删除推荐缓存。
### 9.9 通知中心、活动记录与讨论区优化

为进一步缓解数据库直连和列表聚合查询压力，后端在以下三个模块继续完成性能优化：

#### 通知中心短缓存

- `GET /notification/page`
  按 `userId + current + size + readStatus` 维度缓存通知分页结果，TTL 为 2 分钟。
- `GET /notification/unread-count`
  按 `userId` 维度缓存未读数量，TTL 为 2 分钟。
- 新增通知、单条已读、全部已读后自动删除通知分页缓存和未读数缓存。

#### 活动报名记录与签到记录优化

当前已优化：

- `myEnrollments()`
  先批量收集活动 ID，再批量查询活动信息，避免逐条 `selectById` 查询活动标题。
- `myCheckins()`
  同样改为批量查询活动信息后再组装返回值。

这样可降低个人活动记录页的数据库往返次数。

#### 讨论区分页接口优化

原讨论区分页存在多层 N+1：

- 每条帖子查询一次作者
- 每条帖子查询一次评论列表
- 每条评论查询一次评论用户
- 每条评论统计一次点赞数
- 每条帖子和评论再分别查询当前用户点赞状态

当前已重构为：

- 分页先查帖子
- 批量查帖子作者
- 批量查帖子评论
- 批量查评论用户
- 批量查帖子点赞关系
- 批量查评论点赞关系和评论点赞数
- 最后统一组装 `DiscussionPostVO`

同时：

- `GET /discussion/page`
  已按 `current + size + keyword + userId` 维度接入 Redis 缓存，TTL 为 5 分钟。
- 发帖、评论、点赞、删帖、删评、置顶、精华、封禁等写操作后统一删除讨论区分页缓存。
### 9.10 缓存失效统一收口

随着缓存覆盖的模块增多，后端已将原本分散在各个业务类中的删缓存逻辑统一收口。

当前新增：

- `CacheEvictService`
- `CacheEvictServiceImpl`

统一负责以下缓存失效：

- 首页统计缓存
- 首页工作台缓存
- 推荐缓存
- 场地分页缓存
- 公告分页缓存
- 公告详情缓存
- 活动列表缓存
- 通知分页缓存
- 通知未读数缓存
- 讨论区分页缓存

当前收口后的收益：

1. 新增写操作时更不容易漏删缓存。  
2. 各业务类不再直接到处写 `scan + delete`。  
3. 首页看板、推荐和业务列表页之间的联动关系更清晰。  
4. 后续继续扩展预约缓存、详情缓存和更多热点接口时，维护成本更低。  

当前已接入统一缓存失效服务的模块包括：

- 场地管理
- 公告管理
- 活动报名与签到
- 通知中心
- 讨论区

这意味着后续若老师或答辩老师追问“缓存一致性怎么做”，可以明确回答：

- 项目采用“数据库更新后删除缓存”的策略；
- 具体缓存清理由统一的 `CacheEvictService` 负责；
- 这样既降低了漏删风险，也提高了后续维护性。
### 9.11 缓存命中日志

为了验证缓存优化是否真正生效，后端在统一缓存工具 `CacheClient` 中增加了缓存行为日志。

当前统一输出的缓存日志包括：

- `CACHE_HIT`
  表示当前请求命中了 Redis 缓存。
- `CACHE_MISS`
  表示当前请求未命中缓存，需要回源数据库。
- `CACHE_HIT_NULL`
  表示命中了空值缓存。
- `CACHE_SET`
  表示数据库回源后成功写入缓存。
- `CACHE_SET_NULL`
  表示空结果被写入空值缓存。
- `CACHE_DELETE`
  表示删除单个缓存。
- `CACHE_DELETE_BATCH`
  表示删除一组缓存。
- `CACHE_DESERIALIZE_FAIL`
  表示缓存反序列化失败，系统会删除异常缓存并回源。

当前设计价值：

1. 便于开发调试时观察缓存是否命中。  
2. 便于答辩时说明 Redis 不只是“写了缓存代码”，而是“能证明缓存在工作”。  
3. 为后续继续补接口耗时日志或命中率统计提供基础。  
### 9.12 控制层瘦身与 Service 分层重构

为了让项目结构更加符合 Spring Boot 分层设计规范，后端已对多个偏“臃肿”的控制器进行重构。

#### 重构前的问题

部分 Controller 存在以下问题：

- 直接持有 Mapper
- 直接编写业务逻辑
- 直接执行缓存读写与缓存失效
- 既负责接请求，又负责核心业务处理

这会带来：

- 分层边界不清晰
- 维护成本高
- 不利于测试与复用
- 不利于答辩时解释系统架构

#### 重构原则

本项目现在统一采用以下控制层规范：

- Controller 只负责：
  - 接收参数
  - 调用 Service
  - 返回统一结果
- Service / ServiceImpl 负责：
  - 核心业务逻辑
  - 权限与业务规则判断
  - 缓存处理
  - 数据组装
  - 与 Mapper 的交互

#### 已完成重构的控制器

当前已完成 Service 化重构的控制器包括：

- `DiscussionController`
- `NoticeController`
- `NotificationController`
- `VenueController`
- `VenueSlotController`
- `RecommendationController`
- `UserController`
- `ActivityController`
- `LogController`
- `FileController`

#### 新增或扩展的服务层

本轮重构中新增或扩展的服务包括：

- `DiscussionService` / `DiscussionServiceImpl`
- `NoticeService` / `NoticeServiceImpl`
- `NotificationService` / `NotificationServiceImpl`
- `VenueService` / `VenueServiceImpl`
- `VenueSlotService` / `VenueSlotServiceImpl`
- `RecommendationService` / `RecommendationServiceImpl`
- `UserService` / `UserServiceImpl`
- `ActivityBizService` / `ActivityBizServiceImpl`
- `LogService` / `LogServiceImpl`
- `FileService` / `FileServiceImpl`

#### 重构后的收益

1. 控制层职责更清晰。  
2. 核心业务逻辑集中在 Service 层，便于维护。  
3. 缓存、权限、聚合查询、数据组装不再散落在 Controller 中。  
4. 代码结构更符合论文答辩中“分层架构设计”的表达方式。  
5. 后续继续扩展模块时，更容易保持一致的工程风格。  
