# 个性化智慧校园助手前端项目文档

## 一、项目概述

### 1.1 项目名称

`campus-assistant-frontend`

### 1.2 项目定位

本项目是“基于 Spring Boot + Vue 的个性化智慧校园助手的设计与实现”的前端工程，采用 Vue 3 技术栈开发，负责页面展示、用户交互、接口调用、权限路由和管理后台界面。

前端只负责展示和交互，不直接访问数据库，不直接连接 Redis 或 Kafka。

## 二、前端技术栈

| 技术栈 | 应用场景 |
| --- | --- |
| Vue 3 | 页面和组件开发 |
| Vite | 前端开发服务器和打包工具 |
| Element Plus | 表格、表单、弹窗、分页、菜单、按钮等 UI 组件 |
| Vue Router | 路由管理、登录拦截、页面跳转 |
| Pinia | Token、用户信息、角色信息、菜单状态管理 |
| Axios | 调用后端 RESTful API |
| ECharts | 数据看板和统计图表 |
| Sass / CSS | 页面样式 |
| Day.js | 时间格式处理 |

## 三、前端项目结构

```text
frontend
├─ public
├─ src
│  ├─ api
│  │  ├─ auth.js
│  │  ├─ user.js
│  │  ├─ role.js
│  │  ├─ notice.js
│  │  ├─ venue.js
│  │  ├─ booking.js
│  │  ├─ activity.js
│  │  ├─ checkin.js
│  │  ├─ recommendation.js
│  │  ├─ dashboard.js
│  │  └─ log.js
│  ├─ assets
│  ├─ components
│  │  ├─ AppHeader.vue
│  │  ├─ AppSidebar.vue
│  │  ├─ AppPagination.vue
│  │  └─ DataCard.vue
│  ├─ layout
│  │  └─ AdminLayout.vue
│  ├─ router
│  │  └─ index.js
│  ├─ stores
│  │  ├─ auth.js
│  │  └─ user.js
│  ├─ utils
│  │  ├─ request.js
│  │  ├─ auth.js
│  │  └─ permission.js
│  ├─ views
│  │  ├─ login
│  │  ├─ dashboard
│  │  ├─ notice
│  │  ├─ venue
│  │  ├─ booking
│  │  ├─ activity
│  │  ├─ checkin
│  │  ├─ recommendation
│  │  ├─ log
│  │  └─ system
│  ├─ App.vue
│  └─ main.js
├─ package.json
└─ vite.config.js
```

## 四、页面模块

### 4.1 登录页

功能：

- 用户登录
- 保存 JWT Token
- 登录成功后跳转首页
- Token 失效后跳回登录页
- 登录后修改密码

接口：

```text
POST /auth/login
GET  /user/current
PUT  /user/password
```

说明：

- 学生账号默认为学号，例如 `23050539414`，初始密码默认为身份证后 6 位。
- 学生登录后可在个人信息页面修改密码；如果后端标记为初始密码状态，前端应提示用户尽快修改密码。
- 教师账号初期暂按系统预置或管理员后台创建处理。
- 管理员账号初期暂按初始化脚本预置超级管理员，再由管理员创建其他管理员处理。
- 系统不开放前台注册入口。

### 4.2 首页数据看板

功能：

- 用户总数
- 公告总数
- 活动总数
- 预约总数
- 签到完成率
- 热点公告图表
- 最近预约趋势

接口：

```text
GET /dashboard/summary
GET /dashboard/trend
GET /dashboard/booking-stat
```

### 4.3 个性化推荐页

功能：

- 根据用户角色展示推荐公告
- 展示推荐活动
- 展示推荐场地
- 展示个性化推荐卡片

接口：

```text
GET /recommendation/personal
```

### 4.4 公告页

功能：

- 公告列表
- 公告详情
- 分类筛选
- 关键字搜索

接口：

```text
GET /notice/page
GET /notice/{id}
```

### 4.5 场地预约页

功能：

- 查看可预约场地
- 选择预约日期和时间段
- 提交预约申请
- 查看预约状态
- 取消未完成预约

接口：

```text
GET  /venue/page
GET  /booking/page
POST /booking
POST /booking/cancel
```

前端提交前需要校验场地、日期、时间段、预约原因等必填项，但最终业务校验以后端为准。

### 4.6 活动页

功能：

- 活动列表
- 活动详情
- 活动报名
- 取消报名

接口：

```text
GET  /activity/page
POST /activity/enroll
POST /activity/cancel
```

### 4.7 签到页

功能：

- 活动签到
- 签到记录查询
- 查看签到状态

接口：

```text
POST /checkin
GET  /checkin/page
```

说明：前端可禁用重复点击按钮，但重复签到的最终拦截必须由后端完成。

### 4.8 日志页

功能：

- 登录日志
- 操作日志
- Kafka 消息日志
- DLQ 补偿日志
- 异常日志

接口：

```text
GET /log/page
GET /log/{id}
```

### 4.9 系统管理页

功能：

- 用户管理
- 角色管理
- 公告管理
- 活动管理
- 场地管理

接口：

```text
GET /user/page
GET /role/page
GET /notice/page
GET /activity/page
GET /venue/page
```

## 五、核心文件说明

| 文件 | 说明 |
| --- | --- |
| `src/main.js` | 前端入口文件，初始化 Vue、Pinia、Router 和全局组件 |
| `src/router/index.js` | 路由配置和路由守卫 |
| `src/stores/auth.js` | Token、登录状态和权限信息 |
| `src/stores/user.js` | 当前用户、角色和菜单权限 |
| `src/utils/request.js` | Axios 请求封装、Token 携带、统一错误处理 |
| `src/utils/permission.js` | 页面访问权限判断 |

## 六、路由与权限设计

| 路由类型 | 说明 |
| --- | --- |
| 公共路由 | 登录页、错误页 |
| 学生路由 | 公告、预约、报名、签到、推荐 |
| 教师路由 | 预约审核、公告发布、统计查看 |
| 管理员路由 | 用户管理、系统管理、日志审计 |

权限控制方式：

- 登录后保存 Token
- 路由守卫判断是否已登录
- 根据角色决定菜单显示
- 后端接口再次校验权限

## 七、前端请求规范

### 7.1 统一请求前缀

```text
/api
```

### 7.2 开发代理

```text
/api -> http://localhost:8085
```

### 7.3 请求约定

- GET 用于查询
- POST 用于新增或提交动作
- PUT 用于修改状态或更新数据
- DELETE 用于删除
- 所有请求统一写在 `src/api`
- 页面组件不直接编写原始 Axios 请求

## 八、页面设计重点

### 8.1 个性化首页

首页根据角色展示不同内容：

- 学生看到公告、活动、预约入口和推荐内容。
- 教师看到审核入口、公告发布入口和统计内容。
- 管理员看到全局数据看板、用户管理和日志审计入口。

### 8.2 可视化展示

使用 ECharts 展示：

- 预约数量趋势
- 签到完成率
- 公告热度
- 活动参与情况

### 8.3 表单与交互规则

- 必填项必须在前端进行基础校验。
- 删除、取消、审核驳回等操作需要二次确认。
- 预约、报名、签到按钮提交后应进入 loading 状态，避免用户重复点击。
- 前端防重复只能改善体验，最终幂等控制以后端为准。

## 九、开发顺序建议

```text
1. 搭建 Vue3 项目基础结构
2. 完成登录页与路由守卫
3. 完成主布局和首页仪表盘
4. 完成公告页与详情页
5. 完成场地预约页
6. 完成活动报名页
7. 完成签到页
8. 完成推荐页
9. 完成日志页和系统管理页
10. 对接后端接口并联调优化
```

## 十、说明

前端项目与后端权限、预约、通知、推荐、统计、日志和补偿能力配套实现。涉及身份判断、重复提交、名额扣减、签到唯一性等关键业务规则时，前端只做交互提示和基础校验，最终以服务端校验结果为准。
