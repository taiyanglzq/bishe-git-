# 个性化智慧校园助手前端项目文档

## 一、项目概述

### 1.1 项目名称

`campus-assistant-frontend`

### 1.2 项目定位

本项目是“基于 Spring Boot + Vue 的个性化智慧校园助手的设计与实现”的前端工程，采用 Vue 3 技术栈开发，负责页面展示、用户交互、接口调用、权限路由、管理后台界面以及 AI 客服入口接入。

前端只负责展示和交互，不直接访问数据库，不直接连接 Redis 或 Kafka。

## 二、前端技术栈

| 技术栈 | 应用场景 |
| --- | --- |
| Vue 3 | 页面和组件开发 |
| Vite | 前端开发服务器和打包工具 |
| Element Plus | 表格、表单、弹窗、分页、菜单、按钮等 UI 组件 |
| Vue Router | 路由管理、登录拦截、页面跳转 |
| Pinia | Token、用户信息、角色信息、菜单状态管理 |
| Axios | 调用后端 RESTful API |
| ECharts | 数据看板和统计图表 |
| AI Chat API | 接入后端 AI 客服助手能力 |
| Sass / CSS | 页面样式 |
| Day.js | 时间格式处理 |

## 三、前端项目结构

```text
frontend
├─ public
├─ src
│  ├─ api
│  │  ├─ auth.js
│  │  ├─ ai.js
│  │  ├─ user.js
│  │  ├─ role.js
│  │  ├─ notice.js
│  │  ├─ venue.js
│  │  ├─ booking.js
│  │  ├─ activity.js
│  │  ├─ checkin.js
│  │  ├─ course.js
│  │  ├─ book.js
│  │  ├─ recommendation.js
│  │  ├─ dashboard.js
│  │  └─ log.js
│  ├─ assets
│  ├─ components
│  │  ├─ AiAssistant.vue
│  │  ├─ AppPagination.vue
│  │  └─ DataCard.vue
│  ├─ layout
│  │  └─ AdminLayout.vue
│  ├─ router
│  │  └─ index.js
│  ├─ stores
│  │  ├─ auth.js
│  │  └─ user.js
│  ├─ utils
│  │  ├─ request.js
│  │  ├─ auth.js
│  │  └─ permission.js
│  ├─ views
│  │  ├─ login
│  │  ├─ dashboard
│  │  ├─ notice
│  │  ├─ venue
│  │  ├─ booking
│  │  ├─ activity
│  │  ├─ discussion
│  │  ├─ course
│  │  ├─ exam
│  │  ├─ book
│  │  ├─ navigation
│  │  ├─ recommendation
│  │  ├─ notification
│  │  ├─ profile
│  │  ├─ log
│  │  └─ system
│  ├─ App.vue
│  └─ main.js
├─ package.json
└─ vite.config.js
```

## 四、页面模块

### 4.1 登录页

功能：

- 用户登录
- 保存 JWT Token
- 登录成功后跳转首页
- Token 失效后跳回登录页
- 登录后修改密码

接口：

```text
POST /auth/login
GET  /user/current
PUT  /user/password
```

说明：

- 学生账号默认为学号，例如 `23050539414`，初始密码默认为身份证后 6 位。
- 学生登录后可在个人信息页面修改密码；如果后端标记为初始密码状态，前端应提示用户尽快修改密码。
- 教师账号初期暂按系统预置或管理员后台创建处理。
- 管理员账号初期暂按初始化脚本预置超级管理员，再由管理员创建其他管理员处理。
- 系统不开放前台注册入口。

### 4.2 首页数据看板

功能：

- 用户总数
- 公告总数
- 活动总数
- 预约总数
- 签到完成率
- 热点公告图表
- 最近预约趋势

接口：

```text
GET /dashboard/summary
GET /dashboard/trend
GET /dashboard/booking-stat
```

### 4.3 个性化推荐页

功能：

- 根据用户角色展示推荐公告
- 展示推荐活动
- 展示推荐场地
- 展示个性化推荐卡片

接口：

```text
GET /recommendation/personal
```

### 4.4 公告页

功能：

- 公告列表
- 公告详情
- 分类筛选
- 关键字搜索

接口：

```text
GET /notice/page
GET /notice/{id}
```

### 4.5 场地预约页

功能：

- 查看可预约场地
- 选择预约日期和时间段
- 提交预约申请
- 查看预约状态
- 取消未完成预约

接口：

```text
GET  /venue/page
GET  /booking/page
POST /booking
POST /booking/cancel
```

前端提交前需要校验场地、日期、时间段、预约原因等必填项，但最终业务校验以后端为准。

### 4.6 活动页

功能：

- 活动列表
- 活动详情
- 活动报名
- 取消报名

接口：

```text
GET  /activity/page
POST /activity/enroll
POST /activity/cancel
```

### 4.7 签到页

功能：

- 活动签到
- 签到记录查询
- 查看签到状态

接口：

```text
POST /checkin
GET  /checkin/page
```

说明：前端可禁用重复点击按钮，但重复签到的最终拦截必须由后端完成。

### 4.8 日志页

功能：

- 登录日志
- 操作日志
- Kafka 消息日志
- DLQ 补偿日志
- 异常日志

接口：

```text
GET /log/page
GET /log/{id}
```

### 4.9 课程查询页

功能：

- 课程卡片网格展示
- 按院系、学期和关键字筛选
- 课程详情弹窗（含基本信息、课程简介、关联考试安排）
- 考试安排显示考试类型标签和详细信息

接口：

```text
GET /course/page
GET /course/{id}
GET /exam/page
```

说明：

- 课程以卡片形式展示，每张卡片显示课程名称、学分、教师、院系、上课时间和地点。
- 点击卡片弹出详情弹窗，同时加载该课程关联的考试安排。
- 考试按类型（期末考试/期中考试）用不同颜色标签区分。

### 4.10 考试安排页

功能：

- 考试列表展示（日期徽章 + 信息卡片）
- 按院系、考试类型和关键字筛选
- 未来 3 天考试高亮和倒计时
- 顶部考试提醒警告条

接口：

```text
GET /exam/page
```

说明：

- 每场考试以横向卡片展示，左侧日期徽章、中间考试信息、右侧倒计时标签。
- 未来 3 天内的考试卡片左侧黄色高亮边框，顶部显示"即将考试"警告条。
- 倒计时显示：今天/明天/N天后/已结束。

### 4.11 图书检索页

功能：

- 图书卡片网格展示
- 按分类和关键字（书名/作者/ISBN）搜索
- 图书详情弹窗（含馆藏信息、借阅状态、内容简介）
- **借阅图书**：详情弹窗内点击"借阅"，库存减 1，创建借阅记录
- **归还图书**：已借阅的图书显示"归还"按钮，归还后库存加 1
- **我的借阅**：页面底部展示当前用户借阅记录

接口：

```text
GET  /book/page
POST /book/borrow
POST /book/return/{bookId}
GET  /book/my-borrows
```

说明：

- 每本图书以"封面+信息"卡片展示，封面使用渐变色占位图。
- 可借册数标签：绿色"可借 N 册"或红色"已借完"。
- 详情弹窗展示馆藏位置、总册数、可借册数、借阅状态和内容简介。
- 已借阅的图书在弹窗中显示"归还"按钮（黄色），未借阅显示"借阅"按钮（蓝色）。
- "我的借阅"区域展示借阅中/已归还记录，借阅中的记录可直接归还。
- 后端使用 Redisson 分布式锁保护借阅/归还并发操作。

### 4.12 考试安排页

功能：

- 考试卡片展示（日期徽章 + 信息卡片）
- 按院系、考试类型和关键字筛选
- 未来 3 天考试高亮和倒计时提醒
- **监考老师**：考试卡片显示监考老师姓名
- **座位号**：考试卡片显示个人座位号（从 ca_exam_seat 表读取）

接口：

```text
GET  /exam/page
GET  /exam/seats/my
```

说明：

- 每场考试以横向卡片展示，左侧日期徽章、中间考试信息、右侧倒计时标签。
- 考试信息包含：时间、地点、院系、监考老师、座位号。
- 座位号由管理员在系统管理 → 考试管理 → 座位管理中分配。

### 4.13 校园导航页

功能：

- 高德地图实时展示（宜春学院校园地图）
- 校园建筑标注（19 个真实建筑位置）
- 建筑分类筛选（教学楼/图书馆/食堂餐厅/体育场馆/行政办公/校门）和搜索
- 点击标注/列表项查看建筑详情
- **当前定位**：浏览器 GPS 定位，蓝色标记显示当前位置

接口：无后端接口，纯前端地图组件。

说明：

- 使用高德地图 JS API v2.0，已配置 API Key。
- 建筑数据为真实的宜春学院地理坐标（19 个地点）。
- 搜索支持自动补全和地理编码搜索（城市：宜春）。
- 地图默认中心点为图书馆坐标。

### 4.13 系统管理页

功能：

- 用户管理
- 角色管理
- 公告管理
- 活动管理
- 场地管理

接口：

```text
GET /user/page
GET /role/page
GET /notice/page
GET /activity/page
GET /venue/page
```

### 4.14 AI 客服助手

功能：

- 为学生、教师、管理员提供统一的悬浮 AI 对话入口
- 支持多轮对话：会话创建、切换、删除，历史消息上下文记忆
- 快捷提问按钮：预约流程、预约状态、报名状态
- 解答系统使用问题和校园常见问题（基于知识库 RAG 检索增强）
- 在前端接收 AI 返回的建议动作，如引导用户跳转到预约页或通知中心
- **多模态交互**：支持文本+图片输入，图片上传（最多4张）、预览和删除

接口：

```text
POST /ai/chat
GET  /ai/sessions
GET  /ai/sessions/{sessionId}/history
DELETE /ai/sessions/{sessionId}
POST /ai/chat/multimodal
```

说明：

- 前端通过 `AiAssistant.vue` 悬浮组件实现，登录后所有页面可见。
- 支持会话列表展开/收起，可切换历史会话和新建对话。
- 发送消息后自动保存 sessionId 至组件状态，后续消息自动关联同一会话。
- 输入框旁新增"图片"按钮，可选取多张图片（最多4张），选中后显示缩略图预览。
- 多模态模式：有图片时调用 `/ai/chat/multimodal`（后端路由至通义千问 VL），纯文本调用 `/ai/chat`（后端路由至 DeepSeek）。
- 后端采用双模型架构：DeepSeek 处理文本对话，通义千问处理多模态图片识别。

## 五、核心文件说明

| 文件 | 说明 |
| --- | --- |
| `src/main.js` | 前端入口文件，初始化 Vue、Pinia、Router 和全局组件 |
| `src/router/index.js` | 路由配置（16个页面路由）和登录守卫 |
| `src/stores/auth.js` | Token、登录状态和权限信息 |
| `src/stores/user.js` | 当前用户、角色和菜单权限 |
| `src/utils/request.js` | Axios 请求封装、Token 携带、统一错误处理 |
| `src/utils/permission.js` | 页面访问权限判断 |
| `src/api/ai.js` | AI 客服、多轮会话、多模态接口封装 |
| `src/api/course.js` | 课程和考试查询接口封装 |
| `src/api/book.js` | 图书检索接口封装 |
| `src/components/AiAssistant.vue` | 悬浮 AI 客服组件，支持多轮对话和图片输入 |

## 六、路由与权限设计

| 路由类型 | 说明 |
| --- | --- |
| 公共路由 | 登录页 |
| 学生路由 | 公告、场地预约、活动签到、课程查询、考试安排、图书检索、校园导航、讨论交流、个性化推荐、通知中心、个人中心 |
| 教师路由 | 公告管理、活动管理、预约审核、课程管理、考试管理、图书管理、统计查看 |
| 管理员路由 | 用户管理、系统管理、日志审计、全部管理功能 |

权限控制方式：

- 登录后保存 Token
- 路由守卫判断是否已登录
- 根据角色决定菜单显示
- 后端接口再次校验权限

## 七、前端请求规范

### 7.1 统一请求前缀

```text
/api
```

### 7.2 开发代理

```text
/api -> http://localhost:8085
```

### 7.3 请求约定

- GET 用于查询
- POST 用于新增或提交动作
- PUT 用于修改状态或更新数据
- DELETE 用于删除
- 所有请求统一写在 `src/api`
- 页面组件不直接编写原始 Axios 请求

## 八、页面设计重点

### 8.1 个性化首页

首页根据角色展示不同内容：

- 学生看到公告、活动、预约入口和推荐内容。
- 教师看到审核入口、公告发布入口和统计内容。
- 管理员看到全局数据看板、用户管理和日志审计入口。

### 8.2 可视化展示

使用 ECharts 展示：

- 预约数量趋势
- 签到完成率
- 公告热度
- 活动参与情况

### 8.3 表单与交互规则

- 必填项必须在前端进行基础校验。
- 删除、取消、审核驳回等操作需要二次确认。
- 预约、报名、签到按钮提交后应进入 loading 状态，避免用户重复点击。
- 前端防重复只能改善体验，最终幂等控制以后端为准。
- AI 对话入口应保持非侵入式，不影响原有主业务流程操作。
- AI 返回内容需要用业务中文展示，不显示底层模型或技术字段。

## 九、开发顺序建议

```text
1. 搭建 Vue3 项目基础结构
2. 完成登录页与路由守卫
3. 完成主布局和首页仪表盘
4. 完成公告页与详情页
5. 完成场地预约页
6. 完成活动报名页
7. 完成签到页
8. 完成推荐页
9. 完成日志页和系统管理页
10. 接入 AI 客服入口
11. 对接后端接口并联调优化
```

## 十、说明

前端项目与后端权限、预约、通知、推荐、统计、日志、AI 客服与内容审核能力配套实现。涉及身份判断、重复提交、名额扣减、签到唯一性以及 AI 审核等关键业务规则时，前端只做交互提示和基础校验，最终以服务端校验结果为准。
