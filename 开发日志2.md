# 开发日志（第二阶段：老师要求的新功能）

---

## 第 30 步：教务系统模拟数据设计（一-5）

目标：
- 为课程查询和考试提醒功能建立数据基础。

完成内容：
- 新增 `ca_course` 表、`ca_exam` 表及对应实体、Mapper、DTO、Service、Controller。
- 预置 7 门课程和 8 条考试数据。
- 教师只能管理本院系课程和考试，管理员可管理全部。

验证结果：后端 `mvn -q -DskipTests compile` 通过。

---

## 第 31 步：课程查询功能（一-1）

目标：
- 为学生、教师和管理员提供课程查询和考试安排查看能力。

完成内容：
- 新增前端 `CourseView.vue`，卡片网格展示，支持院系/学期/关键字筛选。
- 点击可查看课程详情和关联考试安排。
- 新增路由 `/course` 和侧边栏菜单。

验证结果：后端编译通过，前端 `npm run build` 通过。

---

## 第 32 步：考试提醒功能（一-2）

目标：
- 提供考试安排查看页面，考试创建后自动推送通知和考前提醒。

完成内容：
- 后端 `ExamServiceImpl` 增强：考试创建后向同院系学生推送通知，使用 Redisson 延迟队列调度考前提醒。
- 新增前端 `ExamView.vue`，列表卡片展示，未来 3 天考试高亮和倒计时。
- 新增路由 `/exam` 和侧边栏菜单。

验证结果：后端编译通过，前端 `npm run build` 通过。

---

## 第 33 步：图书检索功能（一-3）

目标：
- 提供馆藏图书检索能力，展示馆藏位置和借阅状态。

完成内容：
- 新增 `ca_book` 表及对应实体、Mapper、DTO、Service、Controller。
- 预置 10 本模拟图书，覆盖 4 个分类。
- 新增前端 `BookView.vue`，卡片网格 + 分类/关键字搜索 + 详情弹窗。
- 新增路由 `/book` 和侧边栏菜单。

验证结果：后端编译通过，前端 `npm run build` 通过。

---

## 第 34 步：校园导航功能（一-4）

目标：
- 使用高德地图展示校园建筑位置，提供搜索和路径指引。

完成内容：
- 新增前端 `CampusMapView.vue`，集成高德地图 JS API v2.0。
- 预置 12 个校园建筑标注，支持分类筛选和自动补全搜索。
- 新增路由 `/navigation` 和侧边栏菜单。

验证结果：后端编译通过，前端 `npm run build` 通过。

---

## 第 35 步：校园知识库与 RAG 检索增强（二-3）

目标：
- 构建校园知识库，为 AI 对话提供 RAG 检索增强。

完成内容：
- 新增 `ca_knowledge` 表及对应实体、Mapper、DTO、Service、Controller。
- 预置 15 条知识条目，关键词匹配打分 + Redis 缓存。
- `AiChatServiceImpl` 集成 RAG，检索结果作为 DeepSeek prompt 上下文。
- `AiChatResponse` 新增 `ragUsed` 字段。

验证结果：后端编译通过。

---

## 第 36 步：多轮对话引擎（二-1）

目标：
- 将 AI 对话升级为多轮，支持上下文记忆和会话管理。

完成内容：
- 新增 `ConversationService`，基于 Redis 管理会话和历史消息。
- 重构 `AiChatService` 接口，支持 `chat(AiChatRequest, User)`。
- `AiChatController` 新增会话列表/历史/删除端点。
- 前端 `AiAssistant.vue` 升级为多轮聊天界面。

验证结果：后端编译通过，前端 `npm run build` 通过。

---

## 第 37 步：个性化学习建议与学习资源推荐（二-2 + 二-4）

目标：
- 基于学生数据提供 AI 个性化学习建议，扩展推荐系统。

完成内容：
- 新增 `LearningAdviceBuilder`，新增 `POST /ai/learning-advice` 端点。
- 扩展 `RecommendationServiceImpl`，新增课程、图书、知识库推荐维度。

验证结果：后端编译通过。

---

## 第 38 步：多模态交互（三-1）

目标：
- 支持文本+图片的多模态输入，对接 DeepSeek Vision API。

完成内容：
- 新增 `MultimodalClient`，支持 base64 图片输入，兼容 OpenAI Vision API 格式。
- 新增 `POST /ai/chat/multimodal` 端点。
- 新增 `MultimodalChatRequest` DTO。
- 前端 `ai.js` 新增 `chatWithImage()` 和 `getLearningAdvice()` 方法。

验证结果：后端编译通过，前端 `npm run build` 通过。

---

## 第 39 步：图书借阅与归还功能

目标：
- 在图书检索基础上实现借阅归还完整闭环。

完成内容：
- 新增 `ca_book_borrow` 表、BookBorrow 实体/Mapper/DTO。
- 借阅接口 `POST /book/borrow` + 归还接口 `POST /book/return/{bookId}` + 我的借阅 `GET /book/my-borrows`。
- Redisson 分布式锁保护并发，唯一索引防重复借阅。
- 前端详情弹窗智能切换借阅/归还按钮。
- 新增"我的借阅"区域展示借阅记录。
- 修复 `res.data?.records` → `res.records` 数据显示问题（同步修复课程/考试列表）。

验证结果：后端编译通过。

---

## 第 40 步：公告审核流程与通知

目标：
- 教师发布公告后需审核，审核通过/驳回后通知发布者。

完成内容：
- 教师发布公告默认 status=0（待审核），管理员发布直接通过。
- AI 自动审核：PASS → 自动通过，FLAG → 待人工审核，BLOCK → 自动驳回。
- 管理员可通过/驳回公告。
- 审核结果通过通知中心推送给发布者。

验证结果：后端编译通过。

---

## 第 41 步：AI 多模态修复与双模型架构

目标：
- 解决 DeepSeek 不支持 Vision API 的问题，前端增加图片上传。

完成内容：
- 前端 AiAssistant.vue 新增图片上传、缩略图预览、发送功能。
- 后端双模型架构：DeepSeek 处理文本，通义千问 VL 处理图片。
- 修复 DeepSeek API Key 未配置导致兜底消息的问题。
- 修复知识库 RAG 缓存 LinkedHashMap 类型转换错误。

验证结果：后端编译通过。

---

## 第 42 步：校园导航真实数据与当前定位

目标：
- 替换宜春学院真实坐标，增加 GPS 定位功能。

完成内容：
- 替换 19 个宜春学院真实建筑坐标数据。
- 新增"校门"分类（西门/东门/南门）。
- 新增定位按钮（浏览器 Geolocation API），蓝色标记 + 精度圆圈。
- 更新地图中心点和地理编码城市。

验证结果：前端联调通过。

---

## 第 43 步：删除场地预约/场地资源/活动签到模块

目标：
- 按用户要求移除三个业务模块及所有关联代码。

完成内容：
- 删除 30+ 后端文件（Controller/Service/Entity/Mapper/DTO/VO）。
- 删除 6 个前端文件（页面 + API）。
- 清理 SQL 中 6 张表。
- 清理 AI 对话、推荐系统、首页看板、缓存失效中的关联逻辑。
- 修复 MyBatis-Plus 软删除问题。

验证结果：后端编译通过。

---

## 第 44 步：管理员图书管理

目标：
- 管理员可在系统管理页新增/编辑/删除图书，支持上传封面。

完成内容：
- SystemView 新增图书管理标签页（仅 ADMIN）。
- 支持封面上传、分类选择、库存管理。
- 后台权限改为仅 ADMIN（原为 TEACHER/ADMIN）。
- 新增借阅记录标签页，支持搜索书名/作者/ISBN。

验证结果：后端编译通过。

---

## 第 45 步：考试监考老师与座位管理

目标：
- 考试增加监考老师字段，支持座位生成/编辑/导出和学生端展示。

完成内容：
- Exam 实体新增 invigilator 字段。
- 新增 `ca_exam_seat` 表，支持按教室行列/学号/随机三种模式生成座位。
- 生成预览 → 手动保存 → 导出 Excel（Apache POI）。
- 学生考试页面显示个人座位号（从 ca_exam_seat 表读取）。
- 系统管理新增考试管理标签页（仅 ADMIN）。

验证结果：后端编译通过。

---

## 第 46 步：批量导入学生账号

目标：
- 导入 40 个计算机学院学生账号，用于测试座位分配和登录。

完成内容：
- 新增 `upgrade-students.sql`，学号 23050539401~23050539440。
- 默认密码 123456（BCrypt 加密），院系为计算机学院。
- 已有账号 23050529414 保持不变。

验证结果：SQL 脚本已提交。

---
