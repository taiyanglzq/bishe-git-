# 个性化智慧校园助手系统设计说明书

## 1 引言

### 1.1 编写目的

本文档用于对“基于 Spring Boot + Vue 的个性化智慧校园助手”进行系统设计说明，明确系统的总体架构、模块划分、数据库设计、接口设计、运行环境和异常处理策略，为后续开发实现、测试部署和论文答辩提供统一依据。

### 1.2 读者对象

本文档的主要读者包括：

- 项目开发人员
- 项目指导教师
- 项目测试人员
- 论文评审与答辩教师

### 1.3 术语解释

| 术语 | 解释 |
| --- | --- |
| 智慧校园助手 | 本毕设项目名称，面向学生、教师、管理员提供校园业务服务 |
| JWT | 用于用户登录认证的无状态令牌 |
| Redis | 用于缓存、Token 存储、短期业务数据加速的内存数据库 |
| Redisson | 基于 Redis 的 Java 客户端，用于分布式锁、延迟队列等能力 |
| Kafka | 消息队列，用于异步通知、日志处理和死信补偿 |
| DLQ | 死信队列，用于处理消费失败消息 |
| DTO | 数据传输对象，接收前端请求参数 |
| VO | 视图对象，返回给前端页面的展示数据 |
| AI 客服助手 | 提供系统使用问答、流程引导和个人状态查询的智能模块 |
| AI 内容审核员 | 对讨论区帖子和评论内容进行自动审核的智能模块 |

### 1.4 参考资料

- [1.0系统需求分析报告-个性化智慧校园助手.md](D:/code/ideajavacode/hmdp-plus/bishe/1.0系统需求分析报告-个性化智慧校园助手.md)
- [后端项目文档.md](D:/code/ideajavacode/hmdp-plus/bishe/后端项目文档.md)
- [前端项目文档.md](D:/code/ideajavacode/hmdp-plus/bishe/前端项目文档.md)
- [项目环境与配置文档.md](D:/code/ideajavacode/hmdp-plus/bishe/项目环境与配置文档.md)
- [开发日志.md](D:/code/ideajavacode/hmdp-plus/bishe/开发日志.md)

## 2 项目概述

个性化智慧校园助手系统面向高校日常教学与学生服务场景，围绕“公告通知、场地预约、活动报名、活动签到、通知中心、讨论交流、个性化推荐、系统管理、日志审计、AI 智能服务”等核心业务进行设计与实现。

系统采用前后端分离架构：

- 前端采用 Vue 3 + Vite + Element Plus
- 后端采用 Spring Boot 3 + MyBatis-Plus
- 数据库采用 MySQL 8
- 缓存与并发控制采用 Redis + Redisson
- 异步消息采用 Kafka
- AI 模块接入 DeepSeek API

系统主要角色包括：

- 学生：查看公告、预约场地、报名活动、签到、接收通知、参与讨论、使用 AI 客服
- 教师：审核本院系预约、发布与管理公告活动、管理本院系讨论内容
- 管理员：全局用户管理、资源管理、日志审计、数据看板、异常消息处理

## 3 设计约束

### 3.1 技术约束

- 后端必须基于 Spring Boot 3 和 Java 17 开发
- 数据访问层统一采用 MyBatis-Plus
- 数据库存储采用 MySQL 8，暂不采用分库分表
- 前端统一采用 Vue 3 与 Element Plus
- Redis 除 Token 存储外，还承担热点接口缓存功能
- Kafka 用于异步通知、日志解耦和死信补偿

### 3.2 安全约束

- 所有管理接口必须进行登录和角色权限校验
- 密码必须加密存储，禁止明文保存
- 学生只能访问自己的预约、报名、签到和通知数据
- 教师只能审核本院系学生预约，只能管理自己发布的公告和活动
- 管理员拥有全局管理权限
- 讨论区发帖和评论必须经过 AI 内容审核或规则兜底审核

### 3.3 业务约束

- 同一学生不能重复预约同一场地同一时间段
- 同一学生不能重复报名同一活动
- 同一学生不能重复签到同一活动
- 活动报名人数不能超过活动容量
- 场地时间段库存不足时不允许继续预约
- 预约取消后需要释放对应时间段库存
- 教师不能发布全校活动或全校公告

## 4 设计策略

### 4.1 架构策略

系统采用前后端分离的 B/S 架构，前端负责界面展示与交互，后端负责业务逻辑、权限控制、数据库访问和缓存处理。该策略具有以下优点：

- 前后端职责清晰，便于独立开发与联调
- 前端界面可灵活优化，后端接口复用性高
- 适合后续扩展移动端、小程序或管理后台

### 4.2 分层设计策略

后端采用典型的四层设计：

- Controller 层：接收请求、参数校验、返回统一结果
- Service 层：编写核心业务逻辑
- Mapper 层：访问数据库
- Entity/DTO/VO 层：完成数据对象转换

### 4.3 缓存设计策略

系统采用“读缓存 + 写删缓存”的策略：

- 高频读接口优先查询 Redis
- 缓存未命中时回源 MySQL 并写回 Redis
- 业务写操作后删除相关缓存
- 通过 `CacheEvictService` 统一管理缓存失效逻辑

### 4.4 并发控制策略

对于场地预约、活动报名等并发敏感业务，系统采用：

- 数据库事务保证业务原子性
- Redisson 分布式锁避免超卖
- 重复执行限制避免重复提交

### 4.5 智能模块设计策略

AI 模块独立于传统业务模块，采用单独的 `ai` 包管理：

- AI 客服助手通过独立接口 `/ai/chat` 提供服务
- AI 内容审核员在讨论区发帖、评论流程中接入
- 当未配置大模型时，使用本地敏感词规则兜底

## 5 系统总体设计

### 5.1 体系结构设计

系统总体采用“浏览器前端 + 应用服务层 + 数据与中间件层”的三层结构。

#### 5.1.1 表现层

表现层由 Vue 3 前端组成，负责：

- 登录认证界面
- 首页数据看板与工作台
- 公告、场地、活动、讨论、通知等业务页面
- 系统管理与日志审计页面
- AI 客服助手悬浮入口与对话面板

#### 5.1.2 业务层

业务层由 Spring Boot 后端组成，负责：

- 用户登录认证与权限校验
- 公告、预约、活动、通知、讨论等业务处理
- 数据看板与推荐结果生成
- AI 对话与内容审核逻辑
- Kafka 消息生产与消费

#### 5.1.3 数据与中间件层

数据与中间件层包括：

- MySQL：业务数据持久化
- Redis：缓存、Token、短期状态数据
- Redisson：分布式锁、延迟队列、重复执行限制
- Kafka：异步通知、日志消息、死信补偿

### 5.2 系统功能结构设计

系统按照业务功能划分为 12 个核心模块：

1. 用户认证与权限模块
2. 首页数据看板模块
3. 校园公告与通知模块
4. 场地资源与预约模块
5. 活动报名与签到模块
6. 讨论交流模块
7. 个性化推荐模块
8. 个人中心模块
9. AI 客服助手模块
10. AI 内容审核模块
11. 系统管理模块
12. 日志审计模块

#### 5.2.1 用户认证与权限模块

负责登录、退出、当前用户查询、密码修改、JWT 签发与解析、角色权限判断。

#### 5.2.2 场地资源与预约模块

负责场地展示、时间段库存、预约提交、预约审核、预约取消和库存释放。

#### 5.2.3 活动报名与签到模块

负责活动发布、报名、取消报名、签到、待签到展示和已签到记录查询。

#### 5.2.4 讨论交流模块

负责帖子发布、图片上传、评论、帖子点赞、评论点赞、置顶、精华、封禁等论坛能力。

#### 5.2.5 AI 智能模块

负责 AI 客服问答与讨论区内容审核，形成智慧校园助手的智能入口与安全保障。

### 5.3 功能设计

#### 5.3.1 用户登录功能

用户输入学号、工号或管理员账号以及密码后，系统校验账号合法性、密码正确性和账号状态，认证成功后生成 JWT 并返回用户信息和角色编码。

#### 5.3.2 场地预约功能

学生可查看场地与开放时间段，选择日期、时间段并填写预约原因提交申请；后端校验重复预约和库存后创建预约记录，教师或管理员可后续审核。

#### 5.3.3 活动报名与签到功能

学生可查看活动列表并报名，活动开始前或签到开放时间内可进入待签到活动页面完成签到，系统记录签到状态并发送通知。

#### 5.3.4 公告与评论功能

教师或管理员可发布公告并设置发布范围；用户可查看公告详情、发表评论并接收通知中心消息。

#### 5.3.5 讨论交流功能

学生和教师可发帖、评论、点赞，教师可管理本院系帖子，管理员可进行全站管理和违规用户封禁。

## 5.4 数据库设计

### 5.4.1 逻辑设计

系统数据库采用关系型设计，核心实体包括：

- 用户 `ca_user`
- 公告 `ca_notice`
- 公告评论 `ca_notice_comment`
- 场地 `ca_venue`
- 场地时间段 `ca_venue_slot`
- 场地预约 `ca_booking`
- 活动 `ca_activity`
- 活动报名 `ca_activity_enroll`
- 活动签到 `ca_checkin`
- 通知 `ca_notification`
- 讨论帖子 `ca_discussion_post`
- 讨论评论 `ca_discussion_comment`
- 讨论点赞关系表
- 日志表 `ca_operation_log`

实体之间的主要关系如下：

- 一个用户可以有多条预约记录
- 一个用户可以报名多个活动
- 一个活动可以对应多个报名记录和签到记录
- 一个公告可以有多条评论
- 一个帖子可以有多条评论和多条点赞记录

### 5.4.2 物理设计

数据库采用 MySQL 8 实现，主要设计原则：

- 所有主键统一使用自增 `id`
- 所有业务表统一包含 `createTime`、`updateTime`
- 需要逻辑删除的表统一包含 `deleted`
- 状态字段使用字符串或整数枚举表达
- 不强制使用物理外键，避免复杂耦合

#### 主要数据表说明

| 表名 | 作用 |
| --- | --- |
| `ca_user` | 存储学生、教师、管理员账号信息 |
| `ca_notice` | 存储公告主数据 |
| `ca_notice_comment` | 存储公告评论数据 |
| `ca_venue` | 存储场地基本信息 |
| `ca_venue_slot` | 存储场地日期时间段库存 |
| `ca_booking` | 存储预约记录 |
| `ca_activity` | 存储活动信息 |
| `ca_activity_enroll` | 存储活动报名记录 |
| `ca_checkin` | 存储活动签到记录 |
| `ca_notification` | 存储站内通知 |
| `ca_discussion_post` | 存储讨论帖子 |
| `ca_discussion_comment` | 存储讨论评论 |

## 5.5 系统接口设计

### 5.5.1 外部接口

系统对外主要提供 RESTful HTTP 接口，前端通过 Axios 调用后端接口完成业务交互。

同时，AI 模块通过外部大模型服务接口完成问答与审核能力调用：

- DeepSeek Chat Completions API

### 5.5.2 内部接口

系统内部各模块之间的调用主要通过 Service 层接口完成，例如：

- `AuthService`
- `BookingService`
- `ActivityBizService`
- `DashboardService`
- `NotificationService`
- `CacheEvictService`
- `AiChatService`
- `ContentModerationService`

### 5.5.3 接口清单

#### 认证与用户

- `POST /auth/login`
- `POST /auth/logout`
- `GET /user/current`
- `PUT /user/password`
- `GET /user/page`
- `POST /user`
- `PUT /user`
- `DELETE /user/{id}`

#### 公告与通知

- `GET /notice/page`
- `GET /notice/{id}`
- `POST /notice`
- `PUT /notice`
- `DELETE /notice/{id}`
- `POST /notice/comment`
- `GET /notification/page`
- `GET /notification/unread-count`
- `PUT /notification/read/{id}`
- `PUT /notification/read-all`

#### 场地与预约

- `GET /venue/page`
- `POST /venue`
- `PUT /venue`
- `DELETE /venue/{id}`
- `GET /venue-slot/page`
- `POST /booking`
- `GET /booking/page`
- `PUT /booking/approve`
- `PUT /booking/reject`
- `POST /booking/cancel`

#### 活动与签到

- `GET /activity/page`
- `POST /activity`
- `PUT /activity`
- `DELETE /activity/{id}`
- `POST /activity/enroll`
- `POST /activity/cancel/{activityId}`
- `GET /activity/my-enrollments`
- `GET /activity/my-checkins`
- `POST /checkin`

#### 讨论区

- `GET /discussion/page`
- `POST /discussion/post`
- `POST /discussion/comment`
- `POST /discussion/like/{postId}`
- `POST /discussion/comment/like/{commentId}`
- `DELETE /discussion/post/{id}`
- `DELETE /discussion/comment/{id}`
- `PUT /discussion/pin`
- `PUT /discussion/feature`
- `POST /discussion/ban`

#### AI 模块

- `POST /ai/chat`
- `POST /ai/moderate/preview`

## 6 开发环境的配置

| 类别 | 推荐配置 |
| --- | --- |
| 操作系统 | Windows 10/11 |
| JDK | Java 17 |
| IDE | IntelliJ IDEA 2023 或以上 |
| Node.js | 18.x 或以上 |
| 包管理工具 | Maven 3.9、npm 9+ |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 7 |
| 消息队列 | Kafka 3.x |
| 接口调试 | Apifox / Postman |

开发环境要求：

- 后端能访问本地 MySQL
- Redis、Kafka 可以通过 Docker 启动
- 前端通过 Vite 代理转发 `/api` 请求到后端

## 7 运行环境的配置

| 类别 | 运行配置 |
| --- | --- |
| Web 服务 | Spring Boot 内嵌 Tomcat |
| 数据库 | MySQL 8 |
| 缓存服务 | Redis |
| 分布式增强 | Redisson |
| 消息队列 | Kafka |
| AI 服务 | DeepSeek API |

运行时要求：

- 后端端口默认 `8085`
- Redis 使用 `database=1`
- Kafka 负责异步通知与死信补偿
- 上传文件通过 `/uploads` 静态映射访问

## 8 测试环境的配置

测试环境与开发环境保持基本一致，便于联调和问题复现。

建议配置如下：

| 类别 | 配置 |
| --- | --- |
| 后端 | Spring Boot + Maven |
| 前端 | Vue 3 + Vite |
| 数据库 | MySQL 测试库 |
| 缓存 | Redis |
| 消息中间件 | Kafka |

测试重点包括：

- 登录与权限校验测试
- 预约库存与重复提交测试
- 活动报名与签到边界测试
- 公告与通知流程测试
- 讨论区内容审核测试
- 缓存命中与失效日志验证

## 9 系统出错处理

### 9.1 出错信息处理

系统统一使用 `Result` 对象返回错误信息，错误码主要包括：

- `400` 参数错误或业务校验失败
- `401` 未登录或 Token 失效
- `403` 无权限
- `404` 数据不存在
- `409` 业务冲突
- `500` 系统内部异常

### 9.2 补救措施

针对不同错误，系统采取以下策略：

- 参数校验失败：直接返回错误提示
- 业务冲突：返回明确的业务提示，例如“重复预约”“活动名额已满”
- 消息发送失败：交由 Kafka DLQ 处理
- 缓存反序列化失败：删除异常缓存并回源数据库
- AI 服务不可用：降级为兜底提示或规则审核

### 9.3 系统维护设计

系统在维护性方面采用以下设计：

- 分层结构清晰，便于定位问题
- DTO / VO / Entity 分离，便于后续扩展
- 缓存失效统一通过 `CacheEvictService` 收口
- 缓存行为统一通过 `CacheClient` 输出日志
- 讨论区审核、通知推送、日志处理等模块均可独立维护

## 10 结论

本系统设计说明书围绕个性化智慧校园助手项目，对架构、功能、数据库、接口、环境和异常处理等方面进行了系统说明。系统整体采用前后端分离架构，在满足高校校园业务闭环的同时，引入 Redis、Redisson、Kafka 和 AI 模块提升系统性能、并发控制能力和智能化水平。

从设计上看，系统已经具备以下特点：

- 功能模块完整，覆盖校园主要服务场景
- 架构清晰，适合后续继续扩展
- 性能与缓存设计较为完善
- 权限与业务规则较严谨
- 具备较强的答辩展示价值和工程化特征
